# خارطة تطوير B2-Rent المتكيّفة

**الحالة:** معتمدة للتنفيذ المرحلي

## القرار التقني

تبقى المنصة على بنيتها الحالية: **React 19 + Vite + Express + tRPC + Drizzle**. لا يُعاد بناؤها إلى Next.js ولا تُنشأ مجلدات `app/`، لأن المسارات والمصادقة ولوحات الشركاء والحجوزات موجودة بالفعل ضمن هذه البنية. تستخدم المنصة صفحات `client/src/pages`، ومكوّنات `client/src/components`، وواجهات tRPC في الخادم.

يستمر Manus كبيئة الإنتاج الرسمية. Vercel هو بيئة قبول تجريبية فقط إلى أن يكتمل قبول PostgreSQL والخدمات المستقلة والمصادقة والتخزين والمهام الدورية.

## مواءمة المتطلبات

| المتطلب المرفوع | التنفيذ المتكيّف |
| --- | --- |
| Supabase Database | تظل طبقة PostgreSQL في `b2rent` معزولة وموازية. لا يجري استبدال `DATABASE_URL` أو طبقة MySQL التشغيلية مباشرة. |
| ملفات اتصال Supabase | لا تُنشأ ملفات واجهة تحتوي مفاتيح أو `.env.local` بقيم وهمية. تبقى أسرار الخادم خارج Git، ويستعمل فحص صحة خادمي للقراءة فقط في Preview. |
| البيانات الأولية وصور Unsplash | لا تُحقن بيانات تجارية أو حجوزات وهمية في قاعدة الإنتاج. يمكن استعمال Unsplash كصور توضيحية عامة فقط في واجهات لا تدّعي أنها عروض شريك حقيقية. |
| بطاقات وتفاصيل العروض | تُحسّن مكونات React وصفحات السيارات والعقارات الموجودة بدلاً من إنشاء صفحات Next.js جديدة. |
| الحجز وWhatsApp | يبقى واتساب متاحاً بعد تأكيد الحجز فقط. تُحافظ تواريخ الحجز والتحقق من التعارضات وسياسة الإلغاء على مصدر بيانات موثوق. |
| خصم الباقة المدمجة | لا يُعاد؛ هذا العرض حُذف من متطلبات المنصة. |
| شهادات العملاء | لا تُنشأ أو تُعرض شهادات أو تقييمات مختلقة. يمكن عرض واجهة مراجعات فارغة حتى تتوفر مراجعات مستخدمين حقيقية ومصرح بها. |
| المظهر الليلي والحركة | يجري تدقيق ThemeContext والمكونات الحالية وتحسين التجاوب والحركة مع احترام `prefers-reduced-motion`. |
| SEO والصور | تستخدم المنصة بدائل React/Vite المتوافقة، مثل `OptimizedImage` وبيانات وصفية في `client/index.html` وصفحات المدن، بدلاً من `next/image`. |

## ترتيب التنفيذ

### الأولوية 0 — أسرار وبيئات القبول

1. تدوير كلمة مرور PostgreSQL ومفتاح Supabase الخادمي اللذين ظهرا سابقاً في السجل، قبل أي استخدام إنتاجي.
2. حفظ رابط **Transaction Pooler** الصحيح في `SUPABASE_DB_URL` ضمن Vercel **Preview فقط**، من واجهة Vercel مباشرة ومن دون مشاركة القيمة في المحادثة أو Git.
3. إعادة اختبار `GET /api/health/supabase` و`GET /api/health` و`GET /api/trpc/auth.me` بطلبات قراءة فقط.
4. إبقاء Production في Vercel دون أسرار Supabase إلى أن تكتمل الخدمات المستقلة واختبارات القبول.

### الأولوية 1 — طبقة PostgreSQL المتوازية

1. إنشاء مخطط Drizzle PostgreSQL متوافق مع schema `b2rent` من دون تعديل المسار التشغيلي الحالي.
2. إنشاء repository أو adapter مستقل لقراءات القبول وعمليات CRUD المحدودة، مع اختبارات عزل schema.
3. معالجة الفروقات الخاصة بـ MySQL، مثل `onDuplicateKeyUpdate` والتعدادات والطوابع الزمنية، قبل تبديل أي مسار تشغيلي.
4. لا تُنسخ أي بيانات تشغيلية أو بيانات مستخدمين قبل موافقة صريحة وخطة رجوع قابلة للاختبار.

### الأولوية 2 — تجربة العرض والحجز

1. تدقيق `OptimizedImage` وبطاقات العروض لضمان نص بديل وحالة تحميل وخطأ وصور بقياس مناسب.
2. تدقيق نموذج الحجز والتقويم وحساب السعر والتحقق من فترة التوفر قبل أي إنشاء حجز.
3. الإبقاء على بوابة CMI المحاكاة كواجهة اختبار حتى توقيع عقد مزود دفع حقيقي والامتثال للمتطلبات القانونية.
4. إطلاق رابط WhatsApp فقط عندما تصبح حالة الحجز مؤكدة، وبنص لا يكشف بيانات حساسة غير لازمة.

### الأولوية 3 — الواجهة والجودة

1. اختبار الصفحات العامة ولوحات الدور على الهاتف وسطح المكتب، مع دعم RTL/LTR واللغات العربية والفرنسية والإنجليزية.
2. تحسين SEO المحلي وOpen Graph وrobots وsitemap ضمن نمط Vite الحالي.
3. قياس حجم الحزم والصور وتحسين التحميل المؤجل دون وضع وسائط كبيرة داخل المشروع.
4. الحفاظ على إمكانية الوصول: تنقل بلوحة المفاتيح، تركيز مرئي، تباين نصي، ورسائل خطأ قابلة للفهم.

## قيود لا تُتجاوز

- لا توضع مفاتيح Supabase أو كلمات المرور أو JWT أو روابط اتصال في Git أو الواجهة أو السجلات أو ملفات ZIP.
- لا تُنشأ مراجعات أو تقييمات أو شهادات عملاء وهمية.
- لا تُنفذ عمليات حجز أو دفع أو نسخ بيانات أو ترحيل إنتاجي دون موافقة صريحة.
- لا يُعلن Vercel كإنتاج كامل ولا يُزال شريط النسخة التجريبية قبل اكتمال متطلبات الاستقلال الخادمي.

## معايير القبول المرحلية

| المرحلة | معيار الإغلاق |
| --- | --- |
| Preview Supabase | حالة `ready` من فحص خادمي آمن، بلا أسرار أو صفوف أو تغييرات بيانات. |
| طبقة PostgreSQL | اختبارات TypeScript وVitest وبناء ناجح، مع عمليات قبول معزولة في schema `b2rent`. |
| الواجهة والحجز | حالات تحميل/خطأ/فراغ متاحة، واختبارات وظيفية للمسارات والحجز دون تعارض. |
| النشر | GitHub Actions أخضر، Vercel Preview مقبول، وManus يبقى النسخة الرسمية حتى استيفاء الخدمات الخارجية. |
