# تقرير فحص الجاهزية الإجمالية لمنصة B2-Rent

**تاريخ الفحص:** 26 أغسطس 2026  
**النطاق:** الكود الحالي، المعاينة المحلية، والنسخة المنشورة على [b2-rentmorocco.vercel.app](https://b2-rentmorocco.vercel.app).  
**حدود الفحص:** لم يتم إنشاء إعلان حقيقي، ولم يتم رفع صور إلى الإنتاج، ولم يتم إنشاء حجز أو دفع أو إرسال رسالة WhatsApp. لذلك يميّز التقرير بين ما أثبته الكود والاختبارات، وما يتطلب حساباً مستخدماً وبيانات إنتاجية أو خدمة خارجية.

## الخلاصة التنفيذية

المنصة في وضع **Demo/Preview قابل للعرض**، وليست بعد في وضع إطلاق تجاري كامل لاستقبال أول إعلان وحجز حقيقي. البنية العامة، المسارات القانونية، وضع العرض التجريبي، المصادقة، لوحات الإدارة/الوكالة، الإشعارات، الفواتير، سياسة الإلغاء، iCal، والقوالب الأساسية موجودة ومدعومة باختبارات. لكن توجد ثلاث فجوات عالية الأهمية قبل الإطلاق: **عدم تطابق حالات الإعلانات مع القيمة المطلوبة `active`**، و**فحص الصور ليس إثباتاً فعلياً شاملاً لأصل الصورة**، و**النسخة المنشورة لا تزال تحتوي على فجوات ترجمة/توقعات اختبارات قديمة**. كما أن التحقق الحي للنشر والرفع يحتاج حساباً وصلاحيات وبيانات اختبار آمنة.

> **الحكم العام:** جاهزية العرض والـDemo جيدة، أما جاهزية السوق الحقيقي والحجوزات الحقيقية فهي **غير مكتملة** حتى معالجة عناصر P0 وP1 أدناه.

## 1. مصفوفة الحالة

| المجال | النتيجة المثبتة | الحالة | الأولوية |
|---|---|---:|---:|
| تشغيل التطبيق والبناء | `pnpm check` مرّ، والبناء الإنتاجي مرّ في سجل التدقيق؛ يوجد تحذير pnpm عن انتقال إعدادات `patchedDependencies/overrides` | مستقر مع تنظيف مطلوب | P2 |
| الاختبارات | Vitest شغّل 143 اختباراً تقريباً في الحالة المرجعية، لكن سجل التدقيق الأخير أظهر 6 إخفاقات مرتبطة بتوقعات شعار/نصوص قديمة بعد التحديث | غير مغلق | P1 |
| Demo Mode | صفحات السيارات/العقارات التجريبية تفتح تفاصيل صحيحة ولا تعرض مسار حجز/دفع حقيقياً للعروض الاحتياطية | مستقر | P0 |
| الصفحات القانونية | `/terms` و`/privacy` تعملان على Vercel وتعرضان محتوى قانونياً فعلياً | مستقر جزئياً | P1 |
| الترجمة | مزود اللغة يعمل والتبديل يغيّر أجزاء كبيرة من الرأس والتذييل؛ بقي محتوى قانوني وأجزاء مرئية لا تغطيها الترجمة بالكامل | غير مكتمل | P1 |
| النشر المباشر | مخطط الإعلانات يستخدم `Published` كقيمة افتراضية، بينما حالات النظام هي `Pending/Approved/Available/Rented/Rejected/Published`؛ لا توجد قيمة `active` حرفياً | غير مطابق للمطلب | P0 |
| فحص الصور | خدمة `imageVerification` موجودة واختباراتها تمر، لكن لا يثبت التدقيق وجود كشف موثوق للصور المنسوخة أو المولدة بالذكاء الاصطناعي على كل رفع | غير مثبت إنتاجياً | P0 |
| WhatsApp | صفحة التفاصيل تبني رابط `api.whatsapp.com/send` برسالة تحتوي العنوان والسعر والمدينة والرابط الحالي؛ لم يتم إرسال الرسالة | مثبت برمجياً | P1 |
| البحث والفلترة | فلترة المدينة والسعر والترتيب موجودة في الواجهة؛ الحقول المطلوبة لناقل الحركة والوقود والوديعة ليست موصولة كفلترة مكتملة في المسار الظاهر | جزئي | P0 |
| الاستجابة الهاتفية | المعاينات الهاتفية والحاسوبية نجحت بصرياً دون كسر واضح في الرأس والبطاقات والمساعد | مستقر بصرياً | P1 |
| قاعدة البيانات/الخدمات الخارجية | اتصال Supabase الصحي وواجهات التخزين الخاصة ممثلة باختبارات، لكن لا يثبت ذلك جاهزية تدفق رفع إنتاجي من مستخدم فعلي | يحتاج قبول إنتاجي | P0 |

## 2. الميزات الجاهزة والمستقرة نسبياً

### الهوية والتنقل

الرأس والتذييل يستخدمان أصل الشعار المستضاف، مع دعم RTL ونسخة هاتفية وقوائم خدمات ولوحة حساب. تم توثيق إصلاح الشعار الشفاف، وتظهر الصفحة المنشورة دون الاعتماد على صورة محلية داخل حزمة التطبيق. توجد اختبارات هوية، لكن أربعة اختبارات تقريباً ما زالت تتوقع مسار أصل قديم؛ وهذا خلل في الاختبارات/المراجع وليس دليلاً على عودة الخلفية البيضاء.

### وضع العرض التجريبي

التدفق الاحتياطي موحد بين الصفحة الرئيسية والبحث والتفاصيل. العروض التجريبية تفتح صفحات تفاصيل صالحة، ولا تُظهر تقويماً أو دفعاً أو حجزاً حقيقياً لها. هذه نقطة حماية مهمة ويجب إبقاؤها حتى وجود قاعدة بيانات ومزود دفع فعليين.

### الصفحات القانونية

المساران `/terms` و`/privacy` مسجلان في `App.tsx` ويعملان في النسخة المنشورة. التذييل يحتوي روابط سياسة الخصوصية وشروط الاستخدام والإشعارات القانونية. محتوى الشروط يتضمن نطاق الوساطة وإخلاء المسؤولية والعمولة، لكن اكتمال اللغات داخل صفحات القانون نفسها غير متساوٍ.

### البنية التشغيلية المساندة

المخطط يتضمن الحجوزات، لقطات سياسة الإلغاء، الفواتير، الإشعارات، القسائم، العقود التجارية، النزاعات، طلبات الاسترداد والتحويل، وتحليلات أحداث الإعلان. وجود هذه الجداول والإجراءات لا يعني أن كل تدفق تجاري جاهز قانونياً أو مالياً للإنتاج، لكنه يثبت وجود أساس تقني قابل للتطوير.

## 3. الثغرات والمشاكل المحتملة مع الحلول

### P0 — توحيد حالة النشر مع المطلب `active`

**الدليل:** `drizzle/schema.ts` يعرّف حالة الإعلان بقيم تبدأ بحروف كبيرة، وقيمة الحالة الافتراضية هي `Published`. كما أن تحديث الإعلان في `server/routers.ts` يعيد بعض الإعلانات إلى `Pending`. هذا يتعارض مع مطلب النشر الفوري بحالة `active`، وقد يسبب اختلافاً بين الواجهة وقواعد الاستعلام إذا كانت بعض الأجزاء تبحث عن `Published` وأجزاء أخرى عن `active`.

**الحل المباشر:** اعتماد enum واحد موثق، ويفضل `active`, `inactive`, `rejected` أو الحفاظ على enum الحالي مع طبقة تطبيع مركزية. إذا كان المطلوب الصريح هو `active`، يجب تعديل المخطط والهجرات وإجراءات `create/update/list` معاً، ثم إزالة أي إعادة تلقائية إلى `Pending` من مسار النشر المباشر. يجب عدم تنفيذ ذلك على قاعدة الإنتاج دون migration آمن واختبار قبول.

**معيار القبول:** إنشاء إعلان اختبار مصادق عليه ينتج حالة `active` في قاعدة البيانات، يظهر في البحث العام فوراً، وتؤدي إعادة التعديل إلى `active` بعد نجاح التحقق؛ ولا توجد مراجعة أدمن في المسار المطلوب.

### P0 — فحص الصور الذكي غير مثبت ككشف أصالة فعلي

**الدليل:** توجد خدمة `server/imageVerification.ts` واختبارات محلية تمر، لكن التدقيق لم يجد إثباتاً تشغيلياً بأن صورة Google أو صورة مولدة بالذكاء الاصطناعي تُكتشف reliably قبل الحفظ في كل مسارات الرفع. اختبار قواعد محلية لا يكفي لإثبات كشف المصدر أو الأصالة.

**الحل المباشر:** تحديد مزود Vision معتمد، وإضافة قرار خادم قبل جعل الإعلان `active`: `accepted`, `rejected`, `needs_review`, مع سبب قابل للعرض. يجب حفظ hash للصورة ونتيجة الفحص ونسخة النموذج ووقت الفحص، وإضافة timeout وfallback آمن يجعل الإعلان غير منشور عند فشل الخدمة. لا ينبغي وعد المستخدم بكشف 100% لأن كشف أصل الصورة يحتاج إشارات متعددة، مثل metadata، perceptual hash، reverse-image provider، ومؤشرات AI، مع مسار مراجعة استثنائي.

**معيار القبول:** ثلاث حالات آلية موثقة: صورة حقيقية مقبولة، صورة اختبار غير أصلية مرفوضة برسالة المستخدم المطلوبة، وفشل مزود الفحص يمنع النشر ولا يحفظ إعلاناً فعالاً. يجب تنفيذ الاختبار في بيئة staging وبصور يملك المشروع حق استخدامها.

### P0 — فلاتر ناقل الحركة والوقود والوديعة غير مكتملة

**الدليل:** Search يعرض فلترة المدينة والسعر والترتيب، بينما الحقول `fuelType` و`transmission` موجودة في المخطط/بيانات العرض، ولا يظهر في الدليل البرمجي مسار متكامل يربطها جميعاً باستعلام عام. كما أن شرط `Avec/Sans caution` ليس مثبتاً كحقل مستقل مكتمل في المخطط الحالي؛ توجد حقول مالية أخرى مثل `deposit` في العقود.

**الحل المباشر:** إضافة مدخلات schema واضحة للسيارة، مثل `fuelType`, `transmission`, `depositRequired`، ثم تمريرها كـinput إلى `listings.list` أو الإجراء المكافئ، وإضافة فهارس مناسبة. يجب توحيد قيم `Manuelle/Automatique` و`Essence/Diesel/Hybride/Electrique` وعدم خلط العربية والفرنسية في القيمة المخزنة.

**معيار القبول:** كل اختيار يغير query المدعوم ويعيد النتائج المطابقة فقط، مع حالة فارغة مفهومة، وزمن استجابة مقبول على بيانات staging، واختبار Vitest/تكامل لكل فلتر.

### P1 — إصلاح الاختبارات الستة الفاشلة وتثبيت عقد الأصول

**الدليل:** سجل `audit_automated_results.txt` أظهر إخفاق اختبارين في `Home.ux.test.ts` وإخفاقات في `Footer.ux.test.ts`, `Navbar.contrast.test.ts`, `branding.test.ts`, و`BrandAsset.hosting.test.ts`. الرسائل تشير إلى توقعات قديمة لمسار الشعار أو عبارات ثابتة لم تعد موجودة بعد التعريب وإعادة بناء المسارات.

**الحل المباشر:** لا تُخفِ الإخفاقات بتعطيل الاختبارات. حدّث fixtures والتوقعات إلى أصل الشعار المستضاف الحالي ومفاتيح الترجمة/النصوص الجديدة، وأضف اختباراً سلوكياً لتغيير `language` بدلاً من اختبار وجود عبارة عربية بعينها في المصدر. بعد ذلك شغّل Vitest كاملاً واحتفظ بسجل النجاح.

**معيار القبول:** صفر اختبارات فاشلة، وعدم وجود assertion يعتمد على نص عربي ثابت عندما يكون النص الآن مترجماً.

### P1 — الترجمة لا تغطي كل الصفحات والمكونات

**الدليل:** المعاينة وVercel أظهرتا أن أجزاء كبيرة من الرأس والتذييل تتبدل، لكن صفحات القانون ومكونات مرئية أخرى يمكن أن تبقى بالعربية عند اختيار الفرنسية أو الإنجليزية. صفحة `/terms` تعمل وتحتوي زر Français، بينما `/privacy` تعمل بمحتوى عربي دون تغطية لغوية مماثلة.

**الحل المباشر:** تمرير `t()` إلى الصفحات القانونية وFAQ والمساعد والمكونات المشتركة، وإضافة قاموس كامل للعنوان والفقرات والروابط ورسائل الحالة. يجب أن يكون اتجاه `dir` على الجذر مرتبطاً باللغة، لا أن يعتمد على CSS ثابت.

**معيار القبول:** عند اختيار كل لغة، يتغير النص الظاهر في الرأس والـHero والبحث وFAQ والتذييل والقانون، ويتغير `document.documentElement.dir` إلى `rtl` للعربية و`ltr` للفرنسية/الإنجليزية دون إعادة تحميل.

### P1 — قبول إنتاجي لمسار الرفع والتخزين

**الدليل:** اختبارات Supabase والتخزين الخاصة تمر، لكن الفحص غير التدخلي لم ينفذ رفع صورة حقيقية بحساب وكالة. كما أن الأسرار ومفاتيح Vision/البريد/الدفع ليست دليلاً على أن كل تدفق خارجي مفعّل في الإنتاج.

**الحل المباشر:** تنفيذ staging acceptance بحساب وكالة محدود الصلاحيات: رفع صورة، رفض MIME غير مناسب، فحص الحجم، تمرير الصورة إلى التخزين الخاص، قراءة الرابط الموقّع، ثم إنشاء إعلان غير منشور عند فشل التحقق. يجب حذف بيانات staging بعد الاختبار وفق سياسة احتفاظ واضحة.

### P2 — تحذير إعدادات pnpm وحجم الحزمة

**الدليل:** سجل TypeScript والاختبارات أظهر تحذيراً بأن حقل `pnpm` في `package.json` لم يعد يقرأ بعض الإعدادات. كما توجد ملاحظة سابقة عن vendor أكبر من 500KB.

**الحل المباشر:** نقل `patchedDependencies` و`overrides` إلى ملف إعداد pnpm المدعوم في الإصدار الحالي، ثم تشغيل `pnpm install --lockfile-only` بحذر ومراجعة lockfile. بعد استقرار السلوك، استخدم dynamic imports وتقسيم الحزم الثقيلة، دون تغيير تدفق الحجز.

## 4. نتيجة كل مطلب من طلب التدقيق

| المطلب | النتيجة |
|---|---|
| نشر الإعلان فوراً بحالة `active` | **غير محقق حرفياً**؛ الموجود `Published` افتراضياً مع حالات أخرى، ويجب توحيدها قبل الإطلاق. |
| رفض صور Google/AI وقبول الصور الحقيقية | **غير قابل للإثبات إنتاجياً حالياً**؛ توجد خدمة واختبارات، لكن يلزم Vision/acceptance حقيقي وسياسة fallback. |
| زر WhatsApp بمعلومات العرض والسعر والتواريخ والرابط | **جزئي**؛ الرابط يبني العنوان والسعر والمدينة والرابط الحالي، لكن الدليل لا يثبت إدراج تواريخ حجز فعلية في الرسالة، ولم يُرسل شيء أثناء التدقيق. يجب إضافة التواريخ عند وجودها أو التصريح بأنها غير متاحة قبل الحجز. |
| فلترة المدينة والسعر | **موجودة في الواجهة** مع ترتيب السعر. |
| فلترة ناقل الحركة والوقود | **الحقول موجودة في النموذج/المخطط، لكن الربط الكامل باستعلام البحث غير مثبت**. |
| فلترة Avec/Sans caution | **غير مثبتة كحقل فلترة مستقل** وتحتاج نموذج بيانات واضحاً. |
| السرعة | **لا يوجد Load Test كمي مثبت في الأدلة المتاحة**؛ يلزم اختبار staging بأرقام p95 وthroughput قبل وعد زمن استجابة. |
| `/terms` و`/privacy` | **تعملان على النسخة المنشورة**، مع فجوة ترجمة داخل المحتوى القانوني. |
| Responsive Mobile | **نجاح بصري مبدئي** على المعاينة الهاتفية، ويستحسن اختبار Safari/Chrome حقيقيين ومقاسات إضافية قبل الإطلاق. |

## 5. خطة الإصلاح المرتبة قبل الإطلاق

**أولاً — P0:** توحيد `active` أو عقد حالات بديل موثق عبر المخطط والخادم والواجهة، وإكمال الفلاتر الخمسة، ثم تثبيت فحص الصور بخدمة Vision وfallback آمن. هذه البنود تؤثر مباشرة في صدق المخزون ومنع نشر إعلان غير موثوق.

**ثانياً — P1:** إصلاح الاختبارات الستة، إكمال i18n للصفحات والمكونات القانونية، وتنفيذ قبول staging لمسار رفع صورة وإنشاء إعلان. بعدها فقط يمكن اختبار زر WhatsApp على تفاصيل إعلان حقيقي دون إرسال الرسالة.

**ثالثاً — P2:** تنظيف إعداد pnpm وتقسيم vendor، ثم تشغيل load test وقياس Core Web Vitals من نسخة قريبة من الإنتاج.

## 6. ما لم يتم تنفيذه عمداً

لم يتم إنشاء مستخدم أو إعلان أو حجز أو فاتورة أو عملية دفع، ولم يتم إرسال رسالة WhatsApp أو بريد، ولم تتم هجرة قاعدة البيانات أو تغيير enum، ولم يتم تفعيل Vision API أو مزود دفع حقيقي. هذه الإجراءات تحتاج بيئة staging، بيانات اختبار يوافق عليها المالك، ومفاتيح خارجية محددة؛ تنفيذها على الإنتاج أثناء التدقيق قد يلوث البيانات أو يسبب التزامات مالية.

## 7. القرار النهائي

**النسخة الحالية مناسبة للعرض التجريبي والمراجعة البصرية، لكنها لا تستحق تصنيف “جاهزة 100% للإطلاق التجاري” بعد.** أقرب مسار آمن هو إغلاق بنود P0 الثلاثة، ثم إصلاح الاختبارات والترجمة، ثم تنفيذ قبول staging موثق. بعد نجاح هذه البوابات يمكن إجراء تدقيق إنتاج أخير محدود، مع إبقاء Demo Mode يمنع الحجز والدفع للعروض الاحتياطية.

### مراجع الأدلة المحلية

- `audit_automated_results.txt` — نتائج TypeScript وVitest والبناء.
- `audit_feature_evidence.txt` — أدلة Search وWhatsApp والمسارات القانونية.
- `audit_schema_search.txt` — مخطط قاعدة البيانات وحالات الإعلان والحقول.
- `audit_visual_findings.md` — نتائج المعاينة الهاتفية والحاسوبية.
- `audit_external_findings.md` — نتائج المعاينة وVercel والمسارات القانونية.
