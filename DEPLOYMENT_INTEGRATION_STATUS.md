# حالة ربط GitHub وVercel

**آخر تحديث:** 24 أغسطس 2026

- مستودع المصدر المعتمد: `https://github.com/kamalbouragba/b2-rent-morocco`
- مشروع Vercel المستهدف: `b2-rentmorocco` ضمن فريق `B2-Rent morocco`.
- تم تسجيل الدخول إلى Vercel بحساب المستخدم وربط تطبيق Vercel في GitHub بوصول محدود إلى المستودع المذكور فقط. أعيد تحميل إعدادات Git في Vercel بعد الحفظ.
- قائمة Git في Vercel ما زالت تعرض نطاق GitHub مختلفاً باسم `Kamalflow660` ولا تعرض المستودع المستهدف بعد إعادة التحميل؛ يلزم مراجعة اتصال حساب GitHub من إعدادات حساب Vercel بدلاً من ربط مشروع بالنطاق الخطأ.
- فتحت إعدادات المصادقة في Vercel لمراجعة الاتصال، ثم تحولت جلسة المتصفح إلى صفحة فارغة مؤقتاً؛ لم تُنفذ أي عملية ربط خاطئة أو نشر.
- تؤكد إعدادات المصادقة في Vercel أن ملف Vercel الحالي يستخدم اتصال GitHub باسم `Kamalflow660` (بينما الحساب البريدي هو `kamalbouragba@gmail.com`). يجب إضافة/تبديل اتصال GitHub إلى `kamalbouragba` قبل أن يمكن اختيار `kamalbouragba/b2-rent-morocco` من قائمة مستودعات Vercel.
- صفحة المصادقة لا تعرض خيار إضافة حساب GitHub ثانٍ؛ تعرض فقط الاتصال الحالي وخيارات Apple/GitLab/Bitbucket/ChatGPT. لذلك قد يتطلب المسار داخل الحساب الحالي فصل GitHub `Kamalflow660` ثم إعادة ربط Vercel بـ`kamalbouragba`، وهو تغيير يجب ألا ينفذ دون موافقة صريحة إضافية لأنه قد يغير النشر التلقائي لمشاريع أخرى.
- وافق المستخدم صراحةً على الفصل. ظهرت قائمة GitHub وفيها `Disconnect`، لكن أدوات التفاعل الآلية لم تثبت الاختيار وأغلقت القائمة قبل التنفيذ؛ لم يتم فصل أي اتصال بعد.
- بعد التأكيد من نافذة Vercel، تم فصل اتصال `Kamalflow660` بنجاح وظهرت رسالة `Login connection removed.`. بدأ الآن إجراء ربط GitHub جديد من حساب Vercel لاختيار `kamalbouragba`؛ لم يتم بعد ربط مستودع بالمشروع أو تشغيل نشر.
- اكتمل ربط حساب GitHub `kamalbouragba` بحساب Vercel بنجاح (`Last used just now`). عند فتح إعدادات Git لمشروع `b2-rent/b2-rentmorocco` ظهر اختيار GitHub ومربع `Save`، لكن الواجهة تحولت إلى صفحة فارغة مؤقتاً قبل فتح قائمة المستودعات؛ لم يُحفظ أي اختيار مستودع بعد.
- في إعدادات Git لمشروع Vercel `b2-rent/b2-rentmorocco` يظهر الآن نطاق `kamalbouragba` والمستودع `b2-rent-morocco` مع زر `Connect`؛ اتصال GitHub الصحيح أصبح متاحاً للمشروع، لكنه لم يُحفظ بعد كاتصال للمستودع.
- اكتمل الربط بنجاح: يعرض مشروع Vercel الآن `kamalbouragba/b2-rent-morocco` بحالة `Connected just now`. تفعّلت مراقبة الـcommit والأحداث المرتبطة بالمستودع. الخطوة التالية هي التحقق من سجل النشر وملاءمة وقت التشغيل ومتغيرات البيئة، لأن التطبيق يعتمد على Express وtRPC وقاعدة بيانات وخدمات آمنة، وليس تطبيق Vite ثابتاً فقط.
- فُتحت صفحة Deployments بعد الربط، لكن واجهة Vercel تحولت إلى صفحة فارغة قبل ظهور قائمة النتائج، لذلك لم يتم تأكيد نجاح أو فشل نشر أولي من المتصفح بعد. الربط نفسه ما زال مؤكداً من صفحة Git.
- التحقق المباشر من `https://b2-rentmorocco.vercel.app/` بتاريخ 2026-08-24 أعاد `404 DEPLOYMENT_NOT_FOUND`؛ لا توجد حالياً نسخة Vercel متاحة على هذا النطاق. هذا لا يلغي نجاح ربط Git، لكنه يؤكد أن إنشاء نشر سليم ما زال خطوة مطلوبة.
- المشروع في Vercel لم يكن مرتبطاً بمستودع عند التحقق ولا يملك نشر إنتاج سابقاً؛ يجري إتمام الربط من إعدادات Git.
- التطبيق الحالي Full-stack ويعتمد على Express وtRPC وOAuth وقاعدة بيانات، لذلك لا يجوز نشره كإخراج Vite ثابت فقط دون إعداد وقت تشغيل الخادم ومتغيرات البيئة اللازمة.

## تقييم الترحيل إلى Vercel

- تؤكد وثائق Vercel أن تطبيق Express يمكن تشغيله كدالة Node، لكن نقطة الدخول الحالية موجودة في `server/_core/index.ts` وتبدأ خادماً عبر `listen()`؛ يلزم تصدير تطبيق Express منفصل وتغيير التكوين قبل أن يصلح كدالة Serverless.
- الملف `vercel.json` الحالي يحدد `outputDirectory: dist/public` فقط، ولذلك يعامل البناء كواجهة Vite ثابتة ولا يشغّل مسارات tRPC أو OAuth أو التخزين.
- تعتمد المصادقة على خدمات OAuth وJWT المهيأة في البيئة الحالية، وتعتمد الصور والمستندات والإشعارات على مفاتيح Forge ووسيط `/manus-storage/*`. لا يجوز نسخ هذه الأسرار إلى GitHub أو الواجهة، ولا يمكن تشغيل هذه الوظائف على Vercel قبل توفير بدائل إنتاجية أو إعداد آمن مكافئ.
- توجد مسارات دورية لتذكير نهاية العقود ومزامنة iCal. يلزم إعادة تهيئتها كمهمات مجدولة مستقلة في Vercel أو إبقاؤها على البيئة الحالية؛ لا تعمل تلقائياً بمجرد نشر الواجهة.
- النتيجة: ربط GitHub وVercel مكتمل كتكامل مصدر، بينما ترحيل بيئة الإنتاج يتطلب قراراً معمارياً وخدمات إنتاجية منفصلة، وليس تعديلاً بسيطاً في `vercel.json`.

## قرار الإنتاج المعتمد

- تعتمد B2-Rent رابط الإنتاج الحالي `https://b2rentmorocc-muehrc85.manus.space` كبيئة التشغيل الرسمية في هذه المرحلة. تم التحقق في 24 أغسطس 2026 من أن الرابط يستجيب بعنوان المنصة الصحيح.
- يبقى مشروع Vercel `b2-rentmorocco` مرتبطاً بالمستودع `kamalbouragba/b2-rent-morocco` للتحكم بالمصدر واستعداد الترحيل؛ لكنه ليس رابط إنتاج معتمداً ما لم يكتمل تكييف الخادم وخدمات التشغيل المستقلة.
- لا تُنقل مفاتيح OAuth أو JWT أو قاعدة البيانات أو التخزين أو Vision AI إلى المستودع أو واجهة العميل. سيجري إدخالها، عند اعتماد الترحيل، في إعدادات بيئة Vercel عبر مسار آمن فقط.

## المتبقي

1. اختيار اعتماد الموقع الحالي كبيئة الإنتاج، أو تنفيذ ترحيل مستقل كامل لخدمات التطبيق إلى Vercel.
2. عند اختيار الترحيل الكامل: توفير قاعدة بيانات وتخزين وبريد/OAuth ومفاتيح Vision AI مستقلة أو متوافقة، وإضافتها إلى Vercel عبر القنوات الآمنة.
3. تكييف نقطة دخول Express وبناء الواجهة وتشغيل المهام الدورية، ثم تشغيل نشر تجريبي وفحص السجلات وكل المسارات الحساسة.
4. بعد نجاح النشر التجريبي، التحقق من أن أي دفع لاحق إلى `main` يطلق نشر Vercel تلقائياً.

## تدقيق نشر Vercel — 24 أغسطس 2026

- تُظهر واجهة Vercel الآن نشرًا بحالة `Ready` و`Latest` للمستودع `main` عند commit `ce3065c`، مع نطاق `b2-rentmorocco.vercel.app` ونطاقات معاينة إضافية.
- تعرض واجهة البناء 3 تحذيرات وتفيد بأن إعدادات هذا النشر تختلف عن إعدادات المشروع الحالية: النشر استخدم إطار `Other`، وأمر بناء `npm run build`، ومجلد إخراج `dist/public`، وأمر تثبيت `npm install --legacy-peer-deps`؛ بينما إعداد المشروع الحالي يعرّف إطار `Vite` ولا يفعّل تجاوزات البناء نفسها.
- لذلك تؤكد حالة `Ready` نجاح بناء الواجهة، لكنها لا تكفي وحدها لإثبات عمل API أو OAuth أو الحجز أو التخزين. ستُفحص سجلات البناء ومسارات الخادم قبل اعتماد هذا النطاق كإنتاج.
- الاختبار المباشر للنطاق `https://b2-rentmorocco.vercel.app/` نجح ويعرض الصفحة الرئيسية العربية وعناصر البحث والتنقل والقوائم والعروض. لا توجد رسالة خطأ مرئية في الصفحة الرئيسية، ما يؤكد أن بناء الواجهة والـSPA متاحان خارجياً.
- اختبار القراءة فقط لمسار `GET /api/trpc/auth.me` على نطاق Vercel أعاد `404 NOT_FOUND` من Vercel في منطقة `iad1`. النتيجة تثبت أن النسخة الحالية تنشر واجهة React فقط ولا تنشر نقطة دخول الخادم أو tRPC؛ لذلك لا يمكن اعتماد النطاق الحالي للإنتاج قبل تكييف الخادم وبيئة التشغيل.

### مرجع التكييف الخادمي

- وفق توثيق Vercel الرسمي: يمكن نشر تطبيق Express عند تصدير كائن `app` افتراضياً من نقطة دخول معتمدة مثل `server.ts` أو `app.ts` أو `src/server.ts`. كما ينبه التوثيق إلى أن `express.static()` لا يخدم الأصول الثابتة في Vercel؛ يجب أن تخرج أصول React إلى المجلد العام المخصص للنشر. المصدر: https://vercel.com/docs/frameworks/backend/express
- وفق توثيق Runtime الرسمي: تدعم Vercel ملفات TypeScript كنقاط دخول خادمية وتعتمد مدير الحزم من ملف القفل؛ وجود `pnpm-lock.yaml` يجعل Vercel يستعمل `pnpm install` تلقائياً، لذا يجب إزالة تجاوز التثبيت بـ npm ومطابقة أوامر البناء مع المشروع. المصدر: https://vercel.com/docs/functions/runtimes/node-js

## إصلاح وقت تشغيل Vercel — 24 أغسطس 2026

- دُفعت إصلاحات فصل تطبيق Express عن أمر `listen()` وإضافة مدخل API خاص بـVercel إلى فرع `main` في commit `3ce1c1e` بعنوان `fix: serve API routes on Vercel`.
- أظهر سجل Deployments في Vercel أن النشر الإنتاجي الجديد بدأ وهو في حالة `Building`، مع رابط معاينة: `https://b2-rentmorocco-qry3g01ca-b2-rent.vercel.app`.
- لا يُعتمد هذا النشر قبل تحوّل الحالة إلى `Ready` واختبار مسار tRPC ومسارات OAuth في وضع القراءة فقط.

## تشخيص تشغيل الدالة — 24 أغسطس 2026

- تم تأكيد وصول إعادة التوجيه `/api/*` إلى دالة `/api/index` بعد أن أعاد نطاق Vercel خطأ `500 FUNCTION_INVOCATION_FAILED` بدلاً من `404` السابق.
- سجل وقت التشغيل للنشر `P1sxcDDw3QaQ2ifBEYuNqJH9ZPtn` يحدد السبب قبل إنشاء تطبيق Express: `ERR_MODULE_NOT_FOUND: Cannot find module '/var/task/server/_core/app' imported from /var/task/api/index.js`.
- السبب المحدد هو أن Vercel أبقى نقطة الإدخال TypeScript كملف ESM مستقل، بينما استيراداً محلياً بلا امتداد (`../server/_core/app`) لم يُجمّع إلى ملف قابل للحل داخل حزمة الدالة. لا يثبت هذا السجل أي فشل في tRPC أو قاعدة البيانات حتى الآن.
- الإصلاح الجاري هو توليد حزمة CJS خادمية صريحة بواسطة `esbuild` أثناء البناء، ثم تحميلها من `api/index.ts` باستخدام `createRequire`، مع تضمين ملف الخط العربي الثابت الذي يحتاجه مولّد عقود الكراء. يضم هذا النهج الاستيرادات الداخلية ومسارات TypeScript aliases ضمن ملف خادمي واحد، ويترك الاعتمادات الخارجية لإدارة حزمة الدالة.
- تؤكد وثائق Vercel أن دوال Node تدعم ملفات TypeScript داخل `/api` لكنها لا تدعم `tsconfig` Path Mappings، وأن الملفات اللازمة يجب أن تدخل حزمة الدالة عبر التحليل الساكن أو `includeFiles`.[1] [2]
- رفض Vercel النشر الأول لإصلاح الحزمة (`355409a`) في تحقق مخطط الإعداد قبل البناء، لأن `functions.api/index.ts.includeFiles` يقبل سلسلة نمط واحدة لا مصفوفة. تم تصحيح الإعداد لاستخدام نمط Glob واحد يجمع `api/_generated/**` و`server/assets/DejaVuSans.ttf`.
- فحص صفحة **Environment Variables** لمشروع `b2-rentmorocco` في Vercel أظهر الرسالة `No Environment Variables Added`. لذلك لا توجد حالياً أي قيم إنتاج لـ`DATABASE_URL` أو`JWT_SECRET` أو OAuth أو Forge على Vercel؛ لا يمكن اعتماد المسارات المحمية أو التخزين أو فحص Vision AI أو قاعدة البيانات على هذا المضيف قبل توفير بدائل إنتاج مستقلة.
- دُفع تصحيح المخطط في الالتزام `2734a30` بعنوان `fix: use valid Vercel includeFiles glob`، وبدأ Vercel بناء نشر إنتاجي جديد بالمعرف `3Q45bae83CXTSDcM4kn6F2U6FevB` في 24 أغسطس 2026. يجب انتظار حالته النهائية ثم اختبار واجهة tRPC للقراءة فقط.
- وافق المستخدم صراحةً على تعطيل Vercel Authentication. تم إيقاف `Require Log In` في إعدادات Deployment Protection ثم إدخال عبارة التأكيد المطلوبة `disable vercel authentication` وحفظ التغيير؛ ظهر تأكيد Vercel بأن المصادقة معطلة. ستُعاد اختبارات HTTP العامة الآن دون تحويل SSO.
- نجح اختبار الوصول العام للنشر `3Q45bae83CXTSDcM4kn6F2U6FevB`: أعاد `/` الحالة `200`، ما يؤكد انتهاء تحويل SSO. لكن طلبَي القراءة فقط `/api/trpc/auth.me` و`/api/oauth/callback` أعادا `500 FUNCTION_INVOCATION_FAILED`. يظهر سجل Vercel أن السبب التالي هو `ERR_REQUIRE_ESM` داخل `/api/index`؛ سيُستخرج اسم الحزمة وسلسلة الاستدعاء الكاملة قبل أي تعديل جديد. كما كشف اختبار `/manus-storage/...` أن إعادة كتابة SPA العامة تبتلع هذا المسار وتعيد `index.html` بدلاً من مسار التخزين، ويجب إصلاحه ضمن تكييف المسارات لاحقاً.
- اكتمل استخراج الخطأ: `api/_generated/app.cjs` يستدعي `require('jose')`، بينما `jose@6.2.9` حزمة ESM فقط. نتج ذلك من استخدام `--packages=external` أثناء حزم الخادم، إذ أبقى اعتماد `jose` خارج الحزمة ثم أجبر CJS على تحميله بـ`require`. التصحيح التالي سيضم الاعتمادات الخادمية في حزمة CJS بدل إبقائها خارجية؛ سيتحقق الاختبار التنفيذي من أن `jose` لا يبقى `require` مستقلاً في الناتج قبل إعادة النشر.
- بعد تضمين الاعتمادات الخادمية، أصبح النشر `8279a7c` جاهزاً وتجاوز الاختبارات العامة: أعاد `/` الحالة `200`، وأعاد طلب tRPC غير الموثق `auth.me` الحالة `200` مع `null` (النتيجة المتوقعة بلا جلسة)، وأعاد مسار OAuth بلا معاملات الحالة `400` برسالة `code and state are required`. تؤكد هذه النتائج إقلاع Express وtRPC وعودة أخطاء إدخال مقصودة بدلاً من `FUNCTION_INVOCATION_FAILED`. ما زال مسار `/manus-storage/*` يتطابق مع إعادة كتابة SPA العامة في الإعداد الحالي، لذلك سيضاف له توجيه صريح إلى الدالة قبل اختباره كمسار خادمي.
- أُرسل توجيه التخزين في الالتزام `1e45c4f`. أعاد رابط المعاينة أثناء حالة `Building` صفحة Vercel الافتراضية لكل المسارات، لذلك لا تمثل نتيجة اختبار التخزين في تلك اللحظة نتيجة تشغيل للتطبيق. سيعاد الاختبار حصراً بعد تحوّل الحالة إلى `Ready`.
- تحوّل النشر `1e45c4f` إلى `Ready`، ونجحت اختبارات القراءة العامة: أعاد `/` الحالة `200` وواجهة tRPC غير الموثقة الحالة `200` مع `null`. أصبح `/manus-storage/vercel-readiness-probe` يمر عبر Express كما هو مقصود، ويعيد `500 Storage proxy not configured`؛ وهي استجابة خاضعة للتحكم تثبت صحة التوجيه وتكشف أن مفاتيح Forge غير متاحة في Vercel. كما أعاد إجراء `listings.list` العام الحالة `200` مع قائمة فارغة. وبما أن لوحة Vercel لا تعرض أي متغير بيئة، فهذا سلوك `getDb()` عند غياب `DATABASE_URL` وليس دليلاً على خلو قاعدة الإنتاج من العروض.

### حالة الاعتماد الإنتاجي

تم إصلاح أخطاء النشر البرمجية التي كانت تمنع إقلاع الدالة: `404` لمسار API، و`ERR_MODULE_NOT_FOUND`، ورفض مخطط `includeFiles`، و`ERR_REQUIRE_ESM` لحزمة `jose`. لا يمكن اعتماد نطاق Vercel كتطبيق B2-Rent كامل حتى توفَّر تكوينات إنتاج مستقلة، لأنها ليست قيماً يمكن استنتاجها أو نسخها من بيئة Manus:

| المتغير | الغرض | الحالة الحالية في Vercel |
|---|---|---|
| `DATABASE_URL` | اتصال MySQL/DSQL متاح شبكياً من Vercel | غير مهيأ |
| `JWT_SECRET` | توقيع جلسات المستخدم | غير مهيأ |
| `VITE_APP_ID` و`OAUTH_SERVER_URL` و`VITE_OAUTH_PORTAL_URL` | OAuth وهوية المستخدم | غير مهيأة |
| `OWNER_OPEN_ID` و`OWNER_NAME` | إشعارات مالك المنصة | غير مهيأة |
| `BUILT_IN_FORGE_API_URL` و`BUILT_IN_FORGE_API_KEY` | تخزين الملفات وVision AI والإشعارات | غير مهيأة |
| إعداد جدولة خارجي أو Vercel Cron | مزامنة iCal وتنبيهات انتهاء العقود | غير مهيأ |

لا ينبغي وضع أي من هذه القيم في GitHub أو في متغير يبدأ بـ`VITE_` ما لم يكن مخصصاً صراحةً للواجهة. يجب إنشاء بدائل إنتاجية مشروعة لخدمات Manus الخاصة، خصوصاً Forge وOAuth، أو إبقاء النسخة المتكاملة على Manus هي الإنتاج المعتمد.

### المراجع

[1]: https://vercel.com/docs/functions/runtimes/node-js "Using the Node.js Runtime with Vercel Functions"
[2]: https://vercel.com/kb/guide/how-can-i-use-files-in-serverless-functions "How can I use files in Vercel Functions?"
