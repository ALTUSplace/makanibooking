# بوابات تفعيل التشغيل الإنتاجي

## الغرض والنطاق

يسجل هذا المستند ما تبقى قبل اعتبار نطاق Vercel بيئة إنتاج كاملة لمنصة B2-Rent. لا يحتوي قيماً سرية أو روابط اتصال أو بيانات مستخدمين. وهو لا يفعّل أي خدمة، ولا يشغّل مهمة مجدولة، ولا ينقل بيانات أو ملفات.

> **القاعدة:** تبقى منصة Manus مرجع الرجوع، وتبقى MySQL/Manus مرجع التشغيل إلى أن يجتاز كل انتقال منفصل في بيئة قبول معزولة ثم يوافق المالك صراحة على توسيع نطاقه.

## تصنيف البنود المتبقية

| البند | الحالة الحالية | ما يمكن تنفيذه دون إذن إضافي | بوابة التفعيل الإلزامية |
| --- | --- | --- | --- |
| متغيرات Vercel الإنتاجية | قاعدة Supabase وتحقق القراءة فقط جاهزان؛ خدمات الهوية والبريد والرؤية غير مستقلة بعد | مراجعة الوثائق وفحوص القراءة العامة فقط | توفير خدمة مستقلة لكل اعتماد وإدخال أسرارها مباشرة في Vercel، من دون مشاركتها في المحادثة أو GitHub |
| اختبار قبول Storage | bucket الخاص وفحص metadata وآلية اختبار مقيدة موجودة؛ الاختبار غير مفعل | فحص الصحة `GET /api/health/storage` فقط | موافقة صريحة على نافذة اختبار واحدة بملف نصي وهمي، ثم إزالة راية التفعيل والتحقق من سجلات معقمة |
| بيانات التشغيل والترحيل | مخطط `b2rent` وطبقة قراءة موازية موجودان؛ لا توجد بيانات منقولة | مراجعة مخطط وحماية القراءة فقط | نسخة احتياطية موثقة، حسابات قبول معزولة، وموافقة صريحة قبل نسخ أي صف أو تغيير مسار قراءة/كتابة |
| OAuth والجلسات | OAuth الحالي مرتبط بالبيئة المتكاملة | اختبارات غير موثقة ومسارات خطأ آمنة | مزود هوية مستقل، callback موثق لنطاق Vercel، سر جلسات مستقل، واختبار CSRF/الدخول/الخروج |
| البريد والرؤية | لا توجد خدمة إنتاج مستقلة موصولة | لا شيء يتجاوز تدقيق الواجهة | مزود بريد مع نطاق مرسل موثق ومزود رؤية مستقل ومفاتيح خادمية لا تبدأ بـ`VITE_` |
| Cron وiCal | المسار محمي وفي وضع جاف؛ لا ينفذ تذكيرات أو مزامنة | التحقق غير الموثق من `401` فقط | سر مجدول داخل Vercel فقط، قاعدة بيانات وهوية مستقلتان، قفل موزع، idempotency، وبيئة اختبار قبل أي تنفيذ فعلي |
| تحويل النطاق الرسمي | Vercel يعرض الواجهة، لكنه ليس نظام الحجز الإنتاجي كاملاً | مراقبة القراءة العامة والبناء | اجتياز قبول Preview للحسابات والملفات والحجز الاختباري والمراقبة، مع خطة رجوع مكتوبة وموافقة صريحة |

## نافذة اختبار Storage الوحيدة المسموح بها

لا يُسمح بتنفيذ اختبار التخزين إلا عند تلقي موافقة صريحة على **نافذة تشغيل واحدة**. يشمل الاختبار ملفاً نصياً وهمياً فقط ولا يستخدم وثائق هوية أو عقوداً أو مرفقات نزاعات.

1. يفعّل المالك الراية الخادمية المؤقتة في إعدادات Vercel، ولا يشارك قيمة `CRON_SECRET` في المحادثة أو الكود.
2. تشغّل Vercel النداء المجدول المصادق عليه مرة واحدة من Production؛ لا يجرى طلب يدوي خارجي.
3. يراجع فقط الأثر المعقم للنتيجة: نجاح/فشل، ومنع الوصول العام، وحصول التنظيف. لا تُسجل أسماء أو مفاتيح أو روابط موقعة.
4. تزال الراية فوراً، ثم ينشر إصدار جديد يعيد المسار إلى الوضع الجاف.
5. تزال آلية القبول المؤقتة من الكود بعد توثيق النتيجة، قبل ربط التخزين بتدفقات المستخدمين.

## تسلسل التفعيل المقترح

1. استكمال خدمات Vercel المستقلة في **Preview**، مع أسرار خادمية جديدة لكل خدمة.
2. تنفيذ اختبارات قراءة وحسابات تجريبية معزولة؛ لا تُستخدم حسابات أو حجوزات حقيقية.
3. تنفيذ اختبار Storage الوهمي المقيد أعلاه بعد موافقة منفصلة.
4. إنشاء نسخة احتياطية قابلة للتحقق، ثم اختبار ترحيل محدود للبيانات في بيئة قبول فقط.
5. تمرير اختبارات الدخول والملفات والحجز والفاتورة والعقد والجدولة مرتين للتحقق من منع التكرار.
6. نقل الإعدادات المختبرة إلى Production، ومراقبة السجلات المعقمة لمدة 24 ساعة، ثم فقط يُطلب اعتماد تحويل النطاق.

## ما لا ينبغي فعله الآن

لا يجوز إضافة أسرار Vercel عبر مستودع Git، أو نقل متغيرات Manus، أو تجربة ملفات حقيقية، أو إدخال `CRON_SECRET` في الرسائل، أو تشغيل ترحيل بيانات، أو تحويل النطاق الرسمي قبل اجتياز بوابات القبول أعلاه.
