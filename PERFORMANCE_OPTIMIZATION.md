# تحسين أداء حزم JavaScript في B2-Rent

## ما تم تنفيذه

- تطبيق التحميل الكسول للصفحات غير الضرورية في أول زيارة، بما في ذلك لوحات التحكم وصفحة المراسلات والمكونات الثقيلة.
- فصل حزم React وReact DOM وRadix والمخططات وPDF والحركة والبيانات عبر إعدادات Rollup في Vite.
- تحويل مولدات الفواتير والعقود والشهادات إلى استيراد ديناميكي عند الضغط على زر التنزيل، بدلاً من تحميل مكتبة PDF مع أول فتح للموقع.
- الإبقاء على الحزم الخاصة بالصفحات داخل ملفات منفصلة حتى لا يدفع المستخدم تكلفة تحميل صفحات لا يزورها.
- الحفاظ على الصور المتجاوبة وlazy-loading وخصائص `width` و`height` و`sizes` الموجودة في البطاقات.

## نتيجة البناء

تم تشغيل `pnpm check` و`pnpm test` و`pnpm build` بنجاح. تم التحقق بصرياً من الصفحة الرئيسية وصفحة البحث على شاشة هاتف بعرض 390 بكسل، كما تم إجراء تدقيق سابق على الحاسوب والهاتف.

أكبر الملفات بعد التقسيم أصبحت منفصلة عن الحزمة الأولية، وأبرزها:

| الملف | الحجم غير المضغوط | الحجم المضغوط gzip |
|---|---:|---:|
| `react-dom-vendor` | 436.65 kB | 131.13 kB |
| `vendor` | 489.01 kB | 146.07 kB |
| `pdf-vendor` | 343.12 kB | 113.04 kB |
| `react-vendor` | 123.93 kB | 34.40 kB |
| `index` | 121.44 kB | 22.72 kB |

هذه الملفات لا تُحمّل كلها عند أول زيارة بالطريقة نفسها؛ التقسيم يسمح للمتصفح بتحميل الجزء المطلوب حسب المسار والميزة.

## ملاحظات قبل التوسع

- ما زال `vendor` العام و`react-dom-vendor` كبيرين نسبياً، لكنهما لا يمنعان البناء أو النشر.
- التحسين التالي عند ارتفاع عدد الزيارات هو تحليل Bundle بصرياً، مراجعة الاستيرادات العامة من مكتبات الأيقونات وRadix، وتفعيل CDN/ضغط Brotli من طبقة الاستضافة عند توفره.
- يجب إعادة قياس الحزم بعد إضافة أي مكتبة جديدة، وعدم استيراد مكتبات PDF أو الخرائط أو الرسوم في ملفات مشتركة تُحمّل في كل المسارات.
- يجب إبقاء اختبارات البناء والتدقيق الهاتفـي جزءاً من كل إصدار قبل النشر.
