# تقرير ضمان الجودة للوحات B2-Rent Morocco

**تاريخ المراجعة:** 22 أغسطس 2026  
**نطاق النسخة:** تدقيق نهائي بعد إزالة البيانات الوهمية وربط الفواتير والملف الشخصي بالبيانات الخادمية.

## نطاق التدقيق

شمل التدقيق لوحتي **Admin** و**Host**، صفحات البحث والحجز والدفع والنجاح والملف الشخصي وحجوزات المستأجر، مركز الدعم، النزاعات، العقود، الفواتير، رفع الصور، التصفح على الهاتف، الروابط الداخلية، وحالات التحميل والخطأ والفراغ. عوملت قاعدة البيانات باعتبارها مصدر الحقيقة، ولم تُنشأ بيانات اختبار إنتاجية أو تُعدّل سجلات حقيقية أثناء الفحص.

## الصلاحيات والأمان

يستخدم الخادم حراس `protectedProcedure` و`ownerProcedure` و`adminProcedure` لعزل العمليات حسب الدور. لا يستطيع المستأجر استدعاء إجراءات الإدارة أو إدارة إعلان مالك آخر، ولا يستطيع المالك الوصول إلى أدوات إدارة المنصة العامة. استجابة `auth.me` منقّاة ولا تعيد `passwordHash` أو الحقول الحساسة.

يبدأ الحجز بحالة `Pending`، ولا ينتقل إلى `Confirmed` إلا عبر مسار المالك الذي يعيد فحص تعارض التواريخ. تُرسل إشعارات الحجز الجديد والقبول أو الرفض في المرحلة المناسبة فقط. إنشاء العقد والفاتورة يعتمد على الحجز والبيانات الخادمية، وليس على إجمالي أو بيانات قابلة للتلاعب من query parameters.

تقوم `payments.create` بمنع الدفع المكرر للحجز، وتستخدم المبلغ المحسوب من الحجز والإعدادات المحفوظة للعمولة وTVA. لا يجمع نموذج Checkout بيانات بطاقة كاملة أو CVV؛ بوابة CMI الحالية محاكاة توضيحية وليست تكاملاً إنتاجياً.

## الفواتير والدفع

أصبح تدفق الدفع من مرحلتين: إنشاء الحجز خادمياً، ثم تسجيل طريقة الدفع المحاكاة. عند نجاح الدفع تُنشأ الفاتورة من إجمالي الحجز المعتمد، وتتاح بياناتها عبر مسار محمي بالملكية أو بدور Admin. يدعم سجل الحجوزات وصفحة الملف الشخصي وصفحة النجاح عرض حالة الدفع والفاتورة وتنزيل PDF مولد عند الطلب من بيانات API المحمية.

يُحسب تفصيل الفاتورة في الخادم مع أساس المبلغ، عمولة المنصة، TVA، والإجمالي النهائي. التحويل البنكي يمكن أن يبقى في حالة انتظار إلى حين المراجعة، بينما لا تُعرض الفاتورة على أنها مدفوعة قبل حالة الدفع المناسبة.

## الوظائف والبيانات

أصبحت صفحات التذاكر والنزاعات متصلة بقاعدة البيانات مع حالات تحميل وخطأ وفراغ واضحة. يدعم النزاع حفظ بيانات المرفقات ورفع الملفات إلى التخزين المدمج، مع إمكانية معالجة التذاكر من لوحة الإدارة.

تم تحويل رفع صور الإعلانات من محاكاة محلية إلى رفع محمي عبر `storage.uploadImage` مع قيود MIME والحجم وعدد الملفات. تمت إزالة المراجعات والتقييمات والنسب الثابتة من الواجهة؛ لا يعرض النظام مراجعة أو تقييماً غير صادر عن بيانات الخادم.

صفحة `Profile` تقرأ الحساب الحالي عبر `auth.me` والحجوزات والفواتير عبر إجراءات tRPC المحمية. وصفحة `MyBookings` تعرض السجل الحقيقي، وحالات الحجز والدفع والفاتورة، وتوفر تنزيل PDF عند توفر فاتورة صالحة. لا توجد أسماء أو حجوزات ثابتة في هذه الصفحات.

## التصفح والواجهة

تم توحيد المسارات القديمة للوحة الشريك، وإصلاح Breadcrumbs وروابط الدعم وصفحات الإشعارات والخصوصية والحجوزات. يعتمد التصميم على Mobile-First وقائمة Burger جانبية ودعم RTL/LTR. شملت المعاينة الهاتفية بدقة 390×844 الصفحة الرئيسية وProfile وMyBookings وCheckout، إضافة إلى Privacy وSupport Tickets وDispute Resolution. ظهر شريط التنقل السفلي بوضوح، وحافظت الصفحات على حالات الفراغ والتحميل والخطأ المفهومة، كما عُرضت تفاصيل النزاع في نافذة آمنة عند طلب المستخدم.

## الاختبارات والأوامر

تم تشغيل الأوامر التالية بنجاح من جذر المشروع:

```bash
pnpm check
pnpm test
pnpm build
```

النتيجة النهائية: **14 ملفات اختبار ناجحاً و41 اختباراً ناجحاً**، مع نجاح فحص TypeScript وبناء Vite والخادم للإنتاج. تشمل الاختبارات المصادقة والأمان، عزل صلاحيات Admin/Host، تدفق الحجز والإشعارات، الفوترة والدفع المحاكى، حماية ملكية الفاتورة، إزالة البيانات الوهمية من Profile وCheckout وSuccess، التذاكر والنزاعات، والعقد.

يصدر البناء تحذيراً معلوماتياً غير مانع لأن بعض حزم vendor أكبر من 500KB بعد التصغير. تم تخفيف الأثر عبر التحميل الكسول وتقسيم الحزم، لكن يمكن مواصلة تحسين LCP على الأجهزة الضعيفة بتقسيم أعمق أو إزالة مكتبات غير مستخدمة.

## التحقق من تدفق Host Approval

تؤكد اختبارات التدقيق أن الحجز يبدأ `Pending`، وأن اعتماد المالك هو النقطة التي تُعاد فيها مراجعة التداخل ويُرسل إشعار القبول للمستأجر. لا يمكن إنشاء عقد مؤكد قبل هذه الخطوة. يلزم اختبار قبول يدوي في staging بحساب مالك وحساب مستأجر منفصلين لتأكيد السلوك على بيانات حقيقية مخصصة، دون استخدام سجلات الإنتاج.

## القيود المعروفة

لم تُنفذ عمليات دفع حقيقية أو إرسال بريد فعلي أو إنشاء سجلات إنتاجية أثناء التدقيق. CMI محاكاة، وإرسال البريد اختياري عند توفر مزود خارجي، وWhatsApp يعتمد على بيانات اتصال صحيحة مرتبطة بالحجز. عقد الكراء قالب آلي يجب مراجعته قانونياً من مختص مغربي قبل اعتماده كوثيقة نهائية.

## الخلاصة

النسخة الحالية مستقرة من ناحية التجميع والاختبارات وحدود الصلاحيات، وأغلقت فجوتي الفواتير والملف الشخصي اللتين كانتا متبقيتين في التدقيق السابق، إضافة إلى حماية صفحة النجاح وعرض تفاصيل النزاع. الحالة المناسبة الآن هي **اختبار قبول staging قبل الإطلاق التجاري**، مع إبقاء الدفع والبريد والتدقيق القانوني كاعتمادات خارجية لازمة قبل الإنتاج الكامل.

## تحديث تدقيق 22 غشت 2026

أُعيدت معاينة الصفحة الرئيسية، البحث، الخصوصية، تذاكر الدعم، النزاعات، وتفاصيل السيارة على الهاتف (390×844) والحاسوب (1280×720). لم تظهر روابط `#` أو أزرار نجاح محلية بلا أثر خادمي في مسارات الإنتاج التي تمت مراجعتها. عُدّل تقويم السيارة ليعرض تواريخ مستقبلية افتراضية، ويؤكد اختيار الفترة فقط، بينما ينتقل الحجز الفعلي عبر صفحة الحجز والدفع إلى إنشاء Booking وPayment وInvoice على الخادم.

أعيد تشغيل `pnpm check` و`pnpm test` و`pnpm build` بعد هذه التغييرات، ونجحت جميعها. ما زال تحذير حجم الحزم بعد التصغير غير مانع للبناء؛ الإعداد الحالي يستخدم التحميل الكسول وتقسيم الحزم، بينما يبقى تحسين LCP عبر تقسيم أعمق بنداً اختيارياً بعد قياس فعلي على أجهزة وشبكات المستخدمين.

## تدقيق واجهة الدفع المحاكية — 22 غشت 2026

تمت إعادة تصميم صفحة Checkout لتقديم تجربة أقرب إلى بوابة دفع مغربية رسمية من حيث التسلسل البصري، مؤشر الثقة، ملخص الطلب، خيارات CMI/البطاقة/التحويل البنكي، وحالات التحميل والخطأ. لا تستخدم الواجهة شعارات CMI الرسمية أو بيانات اعتماد حقيقية، وتبقي تنبيه المحاكاة ظاهراً للمستخدم. تم التحقق من التجاوب على الهاتف والحاسوب، ثم تشغيل `pnpm check` و`pnpm test` و`pnpm build`، مع اختبار تدقيق ثابت في `server/checkout-ui.audit.test.ts`.

هذا التحسين بصري وتجريبي فقط؛ تفعيل المعالجة المالية الحقيقية يتطلب حساب تاجر CMI وبيئة اختبار ومفاتيح إنتاج منفصلة.
