# توثيق اختبار لوحتي Admin وHost

## نطاق التحقق

تم التحقق آلياً من حدود الصلاحيات على مستوى tRPC وHTTP، ومنع المستأجر من إجراءات المالك، ومنع المالك من مسارات الإدارة العامة. تشمل الحدود: مراجعة الإعلانات، الحجوزات، المدفوعات، النزاعات، التوفر، وطلبات السحب.

تم تشغيل الاختبارات التالية بنجاح:

- `server/admin.authorization.test.ts`: اختبارات حارس Admin وHost.
- `server/admin.http.integration.test.ts`: اختبار HTTP للوصول غير المصرح به إلى إجراء Admin.
- `server/panel-flows.authorization.test.ts`: اختبارات حدود التدفقات الأساسية.
- `server/notificationService.test.ts` و`client/src/lib/commercialLease.test.ts`: اختبارات الميزات المرتبطة بالحجز والعقد.

## التحقق المرئي

تمت معاينة لوحة Admin ولوحة Host على الحاسوب والهاتف بعد إضافة المراجعة، إدارة الحجوزات، سجل المدفوعات، طلبات السحب، النزاعات، وتوفر الإعلانات.

## حدود التحقق

اختبارات التدفقات التي تغيّر بيانات الإنتاج، مثل إنشاء إعلان فعلي أو تغيير حالة حجز فعلي أو دفع أموال، لا تُنفذ تلقائياً على قاعدة البيانات لتجنب إنشاء بيانات اختبار أو تعديل بيانات المستخدمين. يجب تنفيذها في بيئة staging ببيانات اختبار مخصصة قبل ربط بوابة دفع حقيقية.

## أوامر التحقق

```bash
pnpm check
pnpm test
pnpm build
```

آخر نتيجة موثقة: فحص TypeScript ناجح، 7 ملفات اختبار ناجحة، 18 اختباراً ناجحاً، وبناء Vite والخادم ناجح.
