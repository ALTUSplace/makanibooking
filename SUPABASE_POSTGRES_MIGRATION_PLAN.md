# خطة ترحيل B2-Rent إلى Supabase PostgreSQL

**الحالة:** مرحلة قبول مستقلة؛ لا يبدل هذا المستند قاعدة بيانات النسخة الرسمية الحالية.

## القرار المعماري

تستخدم النسخة الرسمية الحالية طبقة Drizzle مع MySQL، بينما أُنشئ مخطط PostgreSQL مستقل باسم `b2rent` في Supabase. لا يجوز تبديل متغير قاعدة البيانات وحده، لأن تعريفات الجداول وبرامج التشغيل في Drizzle مرتبطة بنوع قاعدة البيانات. لذلك سيكون الانتقال عبر طبقة PostgreSQL موازية واختبارات قبول، ثم تحويل حركة القراءة والكتابة تدريجياً بعد اكتمال التكافؤ.

> يعتمد Drizzle نهج «المخطط في الكود» أو «المخطط في قاعدة البيانات»، ويدعم توليد ملفات SQL وتطبيقها بصورة خاضعة للتحكم بالإصدارات. [1]

| المرحلة | الإجراء | معيار القبول | الأثر على النسخة الرسمية |
|---|---|---|---|
| 1. العزل | إنشاء `b2rent` ونسخة ترحيل SQL قابلة للمراجعة | جداول وفهارس وسجل ترحيل معزولون | لا تأثير؛ اكتملت |
| 2. التوافق | تعريف `pg-core` موازٍ وطبقة وصول PostgreSQL مستقلة | فحوص schema وCRUD في قبول بلا بيانات إنتاج | لا تأثير |
| 3. الاستيراد | تصدير من MySQL ثم تحميل متدرج مع أرقام صفوف وبصمات تحقق | تطابق العدّ والقيود والمفاتيح الخارجية | لا كتابة على MySQL |
| 4. القبول | تشغيل واجهات القراءة والحجز والحساب على قاعدة قبول PostgreSQL | نتائج متكافئة واختبارات صلاحيات وحجز | لا تغيير في المستخدمين الحقيقيين |
| 5. التحويل | تعيين أسرار Vercel للخادم فقط وإطلاق نشر جديد | health جاهز، OAuth وتخزين وCron مستبدلة بخدمات مستقلة | قرار منفصل بعد الموافقة |

## الاتصال والأسرار

يستخدم Vercel دوال serverless قصيرة العمر؛ لذلك يعتمد التطبيق عند هذه المرحلة رابط **Transaction pooler** من Supavisor على المنفذ `6543`. توصي Supabase بهذا النمط للدوال serverless/edge، مع ملاحظة أن transaction mode لا يدعم prepared statements. [2]

| المتغير | الموضع المسموح | الغرض | محظور |
|---|---|---|---|
| `SUPABASE_DB_URL` | الخادم أو إعداد Vercel المشفّر | اتصال PostgreSQL المجمع | GitHub، الواجهة، سجل التطبيق |
| `SUPABASE_SERVICE_ROLE_KEY` | الخادم فقط | عمليات إدارية محدودة وHealth خادمي | أي متغير `VITE_*` أو كود العميل |
| `SUPABASE_URL` | الخادم أو العميل عند استخدام المفتاح العام المنفصل | عنوان المشروع، بلا سر | إضافة مسارات REST إلى عنوان قاعدة البيانات |

لا تطبَّق متغيرات Vercel على نشرات سابقة؛ يلزم نشر جديد بعد إدخالها، ويُختار نطاق Production/Preview صراحةً. كما تُشفَّر القيم في الراحة، لكنها متاحة لأصحاب الوصول إلى المشروع، لذلك لا تُسجّل ولا تُدفع إلى GitHub. [3]

## ما تم التحقق منه

أعيد تعيين كلمة مرور PostgreSQL بموافقة صريحة من المالك، ثم حفظ رابط Transaction pooler في أسرار الخادم. نجح اختبار قراءة خادمي ولم ينشئ بيانات. طُبق `supabase/migrations/0001_b2rent_schema.sql` داخل معاملة واحدة، وتحقق المخطط من وجود 20 جدولاً و40 فهرساً، فيما بقيت جداول `public` القائمة دون تعديل.

## القيود الحالية

لا يزال التطبيق التشغيلي يستورد جداول MySQL مباشرة، كما يعتمد مصادقة Manus والتخزين وفحص الصور والمهام الدورية. لذلك فإن وجود Supabase PostgreSQL وحده لا يجعل نشر Vercel جاهزاً لتلقي حجوزات حقيقية. لا يُحوَّل الإنتاج إلا بعد تنفيذ المراحل 2–5 مع اختبار قبول مستقل.

## المراجع

[1]: https://orm.drizzle.team/docs/migrations "Drizzle ORM — Migrations"
[2]: https://supabase.com/docs/guides/database/connecting-to-postgres "Supabase — Connect to your database"
[3]: https://vercel.com/docs/environment-variables "Vercel — Environment Variables"
