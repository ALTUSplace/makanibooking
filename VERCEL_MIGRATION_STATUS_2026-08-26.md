# حالة نقل MAKANIbooking إلى Vercel

**التاريخ:** 26 أغسطس 2026  
**النطاق المنشور:** https://makanibooking-morocco.vercel.app/  
**مستودع GitHub:** `kamalbouragba/makanibooking`

## الحالة التنفيذية

تم إنشاء Deployment على Vercel من مستودع GitHub، وتم التحقق من إعدادات Vite، مجلد الجذر، أمر البناء، مجلد الإخراج، وأمر تثبيت الحزم. النسخة المنشورة تعرض الواجهة العامة وتستجيب لمسارات الصحة وواجهة API العامة. كما تم تحديث روابط SEO والنطاقات العامة إلى نطاق Vercel.

نجحت فحوص TypeScript والاختبارات والبناء المحلي قبل حفظ النسخة المستقرة. لا توجد أسرار داخل المستودع أو هذا التقرير.

## ما تم التحقق منه

| المجال | النتيجة |
|---|---|
| GitHub → Vercel | تم إنشاء المشروع من المستودع والفرع `main` عبر واجهة Vercel |
| Vite build | الإعدادات المتحققة: `pnpm build` و`dist/public` |
| API | نقطة `auth.me` تعمل على النطاق المنشور وتعيد حالة عدم وجود جلسة عند عدم تسجيل الدخول |
| صفحات الواجهة | الصفحة الرئيسية و`/terms` و`/privacy` متاحة على النطاق العام |
| SEO | canonical وsitemap وOpen Graph تشير إلى نطاق Vercel الجديد |
| Supabase | يوجد تحقق read-only وتجهيزات عزل، لكن لا يوجد cutover لمسار التشغيل |

## المتطلبات غير المكتملة

### المصادقة
المشروع الحالي مبني على Manus OAuth. يجب توفير `OAUTH_SERVER_URL` و`VITE_OAUTH_PORTAL_URL` و`VITE_APP_ID` من مزود OAuth الحالي، أو تنفيذ ترحيل برمجي مستقل إلى Supabase Auth. لا يجوز وضع قيم تخمينية أو استخدام رابط Supabase مكان قيم Manus OAuth.

### قاعدة البيانات
طبقة التشغيل الحالية ما زالت تستخدم MySQL/Manus، بينما يوجد مخطط PostgreSQL مقترح لـSupabase. لا ينبغي تبديل المحول قبل مطابقة المخطط، تنفيذ ترحيل قابل للرجوع، وأخذ نسخة احتياطية والتحقق منها. لم تُجرَ أي عملية كتابة أو تحويل على بيانات الإنتاج.

### التخزين والبريد والخدمات الأخرى
توجد adapters وفحوص readiness آمنة لـSupabase Storage وبعض الخدمات، لكن اختبار رفع ملف فعلي داخل bucket لم يُنفذ، كما لم يتم تفعيل بريد إنتاجي أو فحص Vision فعلي أو تشغيل Cron إنتاجي مؤثر.

## القرار الآمن

الموقع منشور على Vercel، لكن لا ينبغي اعتباره جاهزاً للحجوزات الحقيقية قبل تفعيل المصادقة، ربط قاعدة بيانات مستقلة والتحقق منها، إضافة خدمات البريد والتخزين المطلوبة، ثم تنفيذ اختبار قبول معزول للحجز والدفع المحاكي.

لا تنفذ حذفاً أو تغييراً في قاعدة بيانات الإنتاج من لوحة Vercel أو Supabase قبل وجود نسخة احتياطية وخطة رجوع وموافقة صريحة على cutover.
