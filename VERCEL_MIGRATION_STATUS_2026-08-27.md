# الحالة التشغيلية لـ MAKANIbooking على Vercel

**التاريخ:** 27 أغسطس 2026  
**النطاق المنشور:** https://makanibooking-morocco.vercel.app/  
**مستودع GitHub:** `kamalbouragba/makanibooking`  
**آخر نسخة محفوظة:** `c5f9bd5e`

## الخلاصة

تم إصلاح عطل الصفحة البيضاء في الإنتاج. السبب العملي كان أن تهيئة عميل Supabase في المتصفح كانت قادرة على رمي استثناء عند غياب أو عدم صحة `VITE_SUPABASE_URL` أو `VITE_SUPABASE_ANON_KEY` قبل تركيب React. أصبحت التهيئة دفاعية، ويستمر التطبيق في العمل مع تسجيل حالة عدم الجاهزية بدلاً من إيقاف bootstrap.

تم نشر الإصلاح تلقائياً بعد حفظ النسخة، ثم التحقق من الصفحة الرئيسية وصفحة التسجيل والمسار المحمي `/my-bookings` والمسارين القانونيين `/terms` و`/privacy`. المسار المحمي يعيد المستخدم غير المسجل إلى `/register?next=/my-bookings` بدلاً من التحميل اللانهائي أو الشاشة الفارغة.

## نتائج التحقق

| المجال | النتيجة |
|---|---|
| React bootstrap | يعمل على الإنتاج بعد حماية تهيئة Supabase |
| الصفحة الرئيسية | ظاهرة على سطح المكتب والهاتف مع RTL وهوية MAKANIbooking |
| صفحة التسجيل | ظاهرة، وحقول البريد وكلمة المرور والموافقة القانونية قابلة للوصول |
| الحماية | `/my-bookings` يعيد المستخدم غير المصادق إلى التسجيل مع مسار عودة آمن |
| الصفحات القانونية | `/terms` و`/privacy` تعملان وتعرضان التذييل والروابط القانونية |
| TypeScript | ناجح: `pnpm check` |
| الاختبارات | ناجحة: Vitest، بما فيها عقد تهيئة Supabase |
| البناء | ناجح: `pnpm build`، مع تحذير حجم bundle غير مانع للنشر |
| API bundle | نجح فحص إنشاء Express application الخاص بـ Vercel |
| GitHub/Vercel | المستودع والفرع `main` مرتبطان، والحفظ الأخير منشور تلقائياً |

## ما يزال خارج التنفيذ الآمن

لا يمكن اعتبار المنصة جاهزة للحجوزات أو الدفع الحقيقي قبل تنفيذ أعمال خارجية تتطلب صلاحية على بيئات الإنتاج وقرار cutover واضحاً: أخذ نسخة احتياطية قابلة للاسترجاع من MySQL/Manus، مطابقة وتنفيذ مخطط PostgreSQL في Supabase، اختبار ترحيل محدود، تفعيل مزود بريد وفحص صور Vision فعلي، ثم إجراء قبول معزول للحجز والدفع قبل تحويل نطاق العمل.

لا توجد أسرار أو مفاتيح حقيقية داخل المستودع أو هذا التقرير. لا ينبغي إنشاء قيم OAuth تخمينية، ولا تبديل قاعدة البيانات أو حذف بيانات الإنتاج من لوحة Vercel أو Supabase دون نسخة احتياطية وخطة رجوع وموافقة صريحة.

## الملاحظة التشغيلية

تظهر في سجل التطوير رسائل `BadRequestError: request aborted` الناتجة عن إلغاء طلبات المتصفح أثناء التنقل، وليست أخطاء bootstrap أو فشل بناء. كما أن تحذير Rollup الخاص بوجود chunks كبيرة يستحق تحسيناً لاحقاً، لكنه لا يمنع التشغيل الحالي.
