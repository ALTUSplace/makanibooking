# سجل قبول Vercel Preview — 24 أغسطس 2026

## نطاق الفحص

| العنصر | الحالة |
|---|---|
| الفرع | `supabase-preview-health` |
| نوع النشر | Vercel Preview فقط |
| الإنتاج الرسمي | Manus — لم يتغير |
| عمليات البيانات | طلبات `GET` وقراءة مخطط فقط |

## النتيجة المرحلية

- أظهر فحص `/api/health/supabase` السابق أن متغير Preview مهيأ، لكن فحص PostgreSQL أعاد حالة غير متاحة.
- سجل التشخيص المقيد احتوى رمز PostgreSQL `28P01` فقط؛ لا تتضمن الاستجابة أو السجل رابط اتصال أو كلمة مرور أو رسالة خطأ خام.
- نجح التحقق الخادمي المحلي بالقراءة فقط، ولذلك تقتصر المعالجة على مواءمة سر `SUPABASE_DB_URL` في Preview عبر واجهة Vercel.
- دفع الالتزام `dc9be48` شغّل نشر Preview جديداً واكتمل بنجاح تقني.
- أعادت إعادة اختبار `GET /api/health/supabase` في النشر الجديد الحالة المختصرة `unavailable` مع وجود الإعداد، وأكد سجل التشغيل الرمز المقيد `28P01` فقط. لا يحوي السجل أو الاستجابة رابط اتصال أو كلمة مرور أو رسالة خطأ خام أو بيانات جداول.
- تعني النتيجة أن قيمة Preview لم تُستبدل بعد بالرابط المتوافق، أو أن اعتمادها ما زال غير مطابق؛ لا يوجد دليل على مشكلة في مخطط `b2rent` أو بياناته.
- فُتح طلب الدمج [#1](https://github.com/kamalbouragba/b2-rent-morocco/pull/1) من فرع القبول إلى `main` لمراجعة التغييرات وتشغيل بوابة الجودة فقط. لا يُدمج الطلب قبل نجاح CI وعودة حالة الصحة `ready` في Preview.
- اكتملت بوابة الجودة في GitHub Actions لطلب الدمج بنجاح في **24 أغسطس 2026**؛ يشمل ذلك فحص الأنواع والاختبارات والبناء. يبقى الدمج معلقاً حصراً إلى حين نجاح اتصال PostgreSQL في Preview.

## معيار الإكمال

يجب أن يعيد المسار السابق حالة نجاح مختصرة بعد اكتمال النشر، ثم تُفحص الصفحة و`/api/health` و`/api/trpc/auth.me` بطلبات قراءة فقط. لا يعني نجاح هذا الفحص اعتماد Vercel كإنتاج؛ تبقى طبقة البيانات التشغيلية MySQL وخدمات الإنتاج الرسمية في Manus.
