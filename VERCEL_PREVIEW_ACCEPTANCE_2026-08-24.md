# سجل قبول Vercel Preview — 24 أغسطس 2026

## نطاق الفحص

| العنصر | الحالة |
|---|---|
| الفرع | `supabase-preview-health` |
| نوع النشر | Vercel Preview فقط |
| الإنتاج الرسمي | Manus — لم يتغير |
| عمليات البيانات | طلبات `GET` وقراءة مخطط فقط |

## النتيجة المرحلية

- أظهر فحص `/api/health/supabase` السابق أن متغير Preview مهيأ، لكن فحص PostgreSQL أعاد حالة غير متاحة.
- سجل التشخيص المقيد احتوى رمز PostgreSQL `28P01` فقط؛ لا تتضمن الاستجابة أو السجل رابط اتصال أو كلمة مرور أو رسالة خطأ خام.
- نجح التحقق الخادمي المحلي بالقراءة فقط، ولذلك تقتصر المعالجة على مواءمة سر `SUPABASE_DB_URL` في Preview عبر واجهة Vercel.
- دفع الالتزام `dc9be48` شغّل نشر Preview جديداً واكتمل بنجاح تقني.
- أعادت إعادة اختبار `GET /api/health/supabase` في النشر الجديد الحالة المختصرة `unavailable` مع وجود الإعداد، وأكد سجل التشغيل الرمز المقيد `28P01` فقط. لا يحوي السجل أو الاستجابة رابط اتصال أو كلمة مرور أو رسالة خطأ خام أو بيانات جداول.
- تعني النتيجة أن قيمة Preview لم تُستبدل بعد بالرابط المتوافق، أو أن اعتمادها ما زال غير مطابق؛ لا يوجد دليل على مشكلة في مخطط `b2rent` أو بياناته.
- فُتح طلب الدمج [#1](https://github.com/kamalbouragba/b2-rent-morocco/pull/1) من فرع القبول إلى `main` لمراجعة التغييرات وتشغيل بوابة الجودة فقط. لا يُدمج الطلب قبل نجاح CI وعودة حالة الصحة `ready` في Preview.
- اكتملت بوابة الجودة في GitHub Actions لطلب الدمج بنجاح في **24 أغسطس 2026**؛ يشمل ذلك فحص الأنواع والاختبارات والبناء. يبقى الدمج معلقاً حصراً إلى حين نجاح اتصال PostgreSQL في Preview.
- اكتملت بوابة الجودة مجدداً بنجاح للالتزام `dc42da2` في **25 أغسطس 2026** بعد تقوية شاشة الدفع وتحسين بيانات SEO الثابتة؛ بقي طلب الدمج غير مدمج، ولم تُجرَ أي كتابة أو ترحيل في Supabase أو Vercel.
- في **25 أغسطس 2026** حدّث مالك المشروع سر `SUPABASE_DB_URL` يدوياً داخل نطاق **Preview** فقط باستخدام رابط Transaction pooler وكلمة مرور PostgreSQL جديدة. لم يتغير `Production` أو `Development`.
- شغّل الالتزام الفارغ `f7b4875` نشر Preview جديداً لفرع `supabase-preview-health`. أعاد `GET /api/health/supabase` بنجاح `HTTP 200` والاستجابة المختصرة `{"ok":true,"service":"b2-rent-supabase-preview","configured":true,"status":"ready"}`.
- أكدت اختبارات القراءة العامة بعد النشر أن `/` أعاد `200` و`/api/trpc/auth.me` أعاد `200/null`. أعاد `/api/health` `503` متوقعاً لخدمات Vercel المستقلة غير المهيأة، ولا يؤثر ذلك في نجاح فحص Supabase أو في إنتاج Manus الرسمي.
- فُحصت استجابة صحة Supabase للتأكد من خلوها من كلمة المرور ورابط PostgreSQL وأسماء المضيفين وصفوف البيانات. لم يُجر أي إنشاء أو تعديل أو حذف في Supabase.

## مرجع اختيار رابط الاتصال

- توصي وثائق Supabase باستخدام **Transaction pooler** للوظائف عديمة الحالة أو Serverless، وهو رابط PostgreSQL على المنفذ `6543`. لا يصلح رابط Direct Connection المعروض على المنفذ `5432` لهذا اختبار Vercel. [Supabase: Connecting to Postgres](https://supabase.com/docs/guides/database/connecting-to-postgres)
- يجب نسخ حقل **Connection string** من نافذة **Connect** بعد اختيار **Transaction pooler**، ولصقه يدوياً في `SUPABASE_DB_URL` بنطاق **Preview** فقط. لا تُسجل قيمة الرابط أو كلمة المرور في Git أو التوثيق أو المحادثة.

## معيار الإكمال

تحقق المعيار بنجاح في Preview: أعاد المسار حالة جاهزية مختصرة بعد اكتمال النشر، ثم فُحصت الصفحة و`/api/health` و`/api/trpc/auth.me` بطلبات قراءة فقط. لا يعني نجاح هذا الفحص اعتماد Vercel كإنتاج؛ تبقى طبقة البيانات التشغيلية MySQL وخدمات الإنتاج الرسمية في Manus.
