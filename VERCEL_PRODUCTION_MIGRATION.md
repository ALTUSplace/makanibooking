# خارطة تشغيل B2-Rent على Vercel بصورة إنتاجية

## الهدف والحالة الحالية

أصبح نشر Vercel قادراً على تشغيل واجهة React وExpress وtRPC، مع مسار صحة آمن يمنع ظهور كتالوج فارغ بسبب غياب قاعدة البيانات. لكن **هذا لا يساوي اعتماد المنصة كإنتاج كامل**: النسخة المنشورة على Vercel لا تملك حالياً قاعدة بيانات أو مفاتيح جلسات أو موفر OAuth أو بدائل مستقلة للتخزين وفحص الصور والجدولة.

> **قاعدة اعتماد:** لا يُحوَّل نطاق Vercel إلى الرابط الرسمي لـB2-Rent قبل أن تمر كل اختبارات القبول الواردة في هذه الوثيقة. تظل النسخة المتكاملة الحالية على Manus هي التشغيل المعتمد إلى ذلك الحين.

## مسارا التشغيل المتاحان

| المسار | ما الذي يستمر في العمل | المقايضة | كلفة الإعداد | التوصية الحالية |
|---|---|---|---|---|
| إبقاء التطبيق المتكامل على الاستضافة الحالية وربط نطاق مخصص بها | قاعدة البيانات، OAuth، التخزين، Vision AI، التنبيهات والمهام الدورية تعمل ضمن البيئة المتكاملة نفسها | لا يكون Vercel هو خادم الإنتاج للتطبيق الكامل | منخفضة؛ يقتصر العمل على النطاق والإعدادات | **المسار الآمن فوراً** |
| ترحيل الإنتاج كاملاً إلى Vercel مع خدمات مستقلة | واجهة عامة سريعة وقابلة للتوسع مع خادم Serverless مستقل | يتطلب ترحيل هوية المستخدم والملفات والذكاء الاصطناعي والمهام الدورية، مع اختبارات قبول كاملة | متوسطة إلى مرتفعة بحسب مزودي الخدمات | مناسب بعد تنفيذ المراحل أدناه |

لا ينبغي خلط المسارين. توجيه المستخدمين إلى نطاق Vercel مع ترك الخدمات غير مهيأة قد يؤدي إلى تسجيل دخول أو حجوزات أو ملفات غير عاملة، حتى لو كانت الصفحة الرئيسية ظاهرة.

## قرار المالك والبنية المستهدفة — 25 أغسطس 2026

اختار المالك أن يصبح **Vercel** بيئة الإنتاج النهائية، مع الاحتفاظ بـ **Manus** للتطوير والإدارة والرجوع الآمن فقط. لا يعني ذلك نقل أسرار Manus أو توجيه النطاق فوراً؛ بل يعني بناء خدمات مستقلة ثم اعتمادها باختبارات قبول منفصلة.

| طبقة الخدمة | الهدف في الإنتاج | الحالة والحدود الحالية |
|---|---|---|
| الواجهة وExpress/tRPC | دوال Vercel Serverless | تعمل تقنياً في Preview وProduction، لكن Production لا يملك بعد جميع خدمات التشغيل |
| البيانات | Supabase PostgreSQL ضمن schema `b2rent` | اتصال Preview read-only مقبول؛ طبقة MySQL الحالية لم تُستبدل ولم تُنقل بيانات |
| هوية المستخدم | مزود مستقل، والمفضل توحيده مع Supabase Auth | لا يزال تسجيل الدخول الحالي خاصاً بـ Manus ويستلزم مسار callback وجلسات جديداً |
| الملفات الحساسة والعامة | Supabase Storage أو مخزن S3 متوافق مستقل | الرفع والتنزيل الحاليان يعتمدان Forge ولا يجوز نقلهما كما هما |
| البريد | موفر بريد إنتاجي مستقل مع نطاق مرسل موثق | لا يجوز مشاركة مفاتيح Manus أو بيانات الحجز في سجلات الفشل |
| فحص أصالة الصور | مزود Vision/LLM مستقل من الخادم | المفتاح والمزود لم يُختارا بعد؛ الرفض الآمن يبقى مطلوباً عند غياب التهيئة |
| iCal والتنبيهات | Vercel Cron أو مجدول خارجي، مع endpoints idempotent | الجدولة الحالية خاصة بـ Manus وتستلزم استبدالاً قبل تحويل الإنتاج |

### قواعد لا يمكن تجاوزها

1. لا تُنسخ أي قيمة `BUILT_IN_FORGE_*` أو أسرار OAuth أو JWT من Manus إلى Vercel.
2. لا تُنقل بيانات المستخدمين أو الحجوزات أو وثائق KYC أو أدلة النزاعات قبل نسخة احتياطية وموافقة صريحة لكل دفعة.
3. لا يتحول النطاق الرسمي قبل نجاح الدخول والملفات والحجز الاختباري والمهام الدورية في Preview مستقل.
4. يبقى نشر Manus مرجع الرجوع إلى أن تستقر مراقبة Vercel بعد التحويل.

## المتطلبات الإنتاجية غير القابلة للنسخ

| المجال | الوضع في الكود الحالي | ما يلزم في Vercel | ضابط أمني |
|---|---|---|---|
| قاعدة البيانات | Drizzle/MySQL و`DATABASE_URL` مع schema Supabase `b2rent` معزول | اعتماد Supabase PostgreSQL عبر adapter موازٍ ثم استبدال تدريجي لمسارات MySQL بعد قبول منفصل | عدم وضع الرابط في Git أو في متغير واجهة أو نقل بيانات قبل نسخة احتياطية |
| الجلسات | `JWT_SECRET` | سر عشوائي إنتاجي مستقل لا يقل عن 32 بايت وتدوير موثق | لا يعاد استخدام سر بيئة Manus |
| هوية المستخدم | Manus OAuth | مزود OAuth مستقل أو مسار ترحيل مدروس من OAuth الحالي، مع عناوين callback لنطاق Vercel | اختبار CSRF وcookies وredirects قبل الإطلاق |
| ملفات المستخدمين | Forge storage و`/manus-storage/*` | تخزين S3-compatible مستقل مع URLs موقعة وسياسات وصول | عدم إتاحة ملفات CNI أو أدلة النزاعات للعامة |
| فحص الصور | Forge Vision AI | مزود رؤية مستقل ومفتاح خادمي، مع حفظ نتيجة الفحص الموقعة | لا يوضع المفتاح في متغير يبدأ بـ`VITE_` |
| البريد والإشعارات | خدمات Manus المدمجة وResend عند التهيئة | موفر بريد ومفاتيح إنتاج وتحقق sender domain | اختبار rate limits وعدم تضمين بيانات حجز حساسة في سجلات الأخطاء |
| iCal وتنبيهات العقود | مسارات دورية ضمن التطبيق | مهمة مجدولة محمية، idempotent، مع قفل موزع | معالجة التكرار والمهام الفائتة صراحةً |

### مصفوفة متغيرات البيئة المستهدفة

تُحفظ القيم ضمن إعدادات مشروع Vercel فقط في بيئتي **Preview** و**Production** المناسبتين، لا في المستودع. توضح وثائق Vercel أن تغير متغيرات البيئة لا يطبَّق على النشرات القديمة، ولذلك يلزم نشر جديد بعد إدخال أو تدوير أي قيمة.[1]

المجموعة الدنيا الحالية التي يعرضها `GET /api/health` بأسماء فقط هي:

```text
DATABASE_URL
JWT_SECRET
VITE_APP_ID
OAUTH_SERVER_URL
BUILT_IN_FORGE_API_URL
BUILT_IN_FORGE_API_KEY
```

هذه الأسماء تصف الاعتمادات الحالية، وليست وصفة لنسخ قيم Manus. قبل الإزالة التدريجية لاعتمادات Manus يجب استبدال الاستخدام في الخادم بخدمات إنتاج مستقلة، ثم إزالة مفاتيح Forge من شرط الجاهزية.

بعد استبدال المكونات، تُستخدم أسماء مستقلة وآمنة، ولا تُدرج قيمها في الوثائق أو Git:

```text
SUPABASE_URL
SUPABASE_DB_URL
SUPABASE_SERVICE_ROLE_KEY
SUPABASE_ANON_KEY
JWT_SECRET
AUTH_REDIRECT_URI
EMAIL_PROVIDER_API_KEY
EMAIL_FROM_ADDRESS
VISION_PROVIDER_API_KEY
CRON_SECRET
```

لا تُضاف هذه القيم إلى Vercel Production قبل أن يكون الكود المقابل قادراً على استخدامها. يوضع كل سر في Production فقط عند الجاهزية، وتُختبر النسخة المطابقة في Preview أولاً.

### تحقق إعدادات المشروع — 25 أغسطس 2026

أظهر فحص صفحة متغيرات البيئة في مشروع `b2-rentmorocco` أن `SUPABASE_URL` و`SUPABASE_DB_URL` موجودان حالياً في نطاق **Preview** فقط. لم تظهر أي قيمة مسجلة لنطاق Production لهذين المتغيرين. هذه ملاحظة حالة فقط؛ لم يُضف أو يُعدّل أي متغير في هذه المرحلة، ولا تتضمن الوثيقة قيم الاتصال أو كلمات المرور.

تؤكد واجهة Vercel أن المتغيرات الحالية من نوع Secret والقيم المحفوظة فيها غير قابلة للاسترجاع من الواجهة. لذلك يُدخل عنوان مشروع Supabase من مصدره المعتمد كقيمة Production مستقلة، بينما يُعاد إدخال رابط Transaction pooler وكلمة المرور الحديثة يدوياً عند إعداد `SUPABASE_DB_URL`؛ لا تُنسخ القيم إلى المستودع أو التوثيق.

في 25 أغسطس 2026 حُفظ `SUPABASE_URL` في نطاق **Production** لمشروع Vercel `b2-rentmorocco`، وأكدت الواجهة نجاح الحفظ وأن نشراً جديداً مطلوب لتطبيقه. لم يتغير `SUPABASE_DB_URL`، وبقي نطاقه **Preview** فقط إلى حين إدخال رابط Transaction pooler الإنتاجي بصورة منفصلة وآمنة.

أظهر نموذج تعديل `SUPABASE_DB_URL` في Vercel أن القيمة السابقة لا يمكن عرضها أو نسخها لأنها Secret للكتابة فقط. لذلك يجب إدخال رابط Transaction pooler كاملاً مع كلمة المرور الجديدة من المالك مباشرة داخل حقل Vercel عند توسيع النطاق إلى Production؛ لا يُقبل نقل هذا السر عبر المحادثة أو مستودع Git.

في تحقق الواجهة اللاحق، بقيت بطاقة `SUPABASE_DB_URL` في قائمة Vercel موسومة بـ **Preview** ومؤرخة بآخر تحديث سابق، بينما ظل نموذج الإدخال مفتوحاً. لذلك لا يوجد إثبات أن قيمة Production حُفظت بعد، ولا يجوز إطلاق نشر Production أو اعتبار اتصال قاعدة البيانات جاهزاً حتى تظهر رسالة نجاح منفصلة للمتغير وتصبح بطاقة النطاق `Production`.

في التحقق النهائي بواجهة Vercel، ظهرت بطاقتا `SUPABASE_DB_URL` و`SUPABASE_URL` معاً تحت نطاق **Production** فقط. بعد نشر Production جديد، أعاد `GET /api/health/supabase` الاستجابة المختصرة `HTTP 200` مع `configured: true` و`status: ready`. كان الطلب للقراءة فقط ولم يعرض رابط اتصال أو كلمة مرور أو صفوف بيانات ولم ينفذ أي كتابة على Supabase.

صُححت تسمية الخدمة في الكود كي تعيد بيئة Vercel `b2-rent-supabase-production` بينما تبقى بيئات المعاينة `b2-rent-supabase-preview`. لا يكشف هذا التمييز تفاصيل اتصال، ويبقى نجاح الاتصال الإنتاجي مثبتاً بالحالة `200/ready` فقط.

بعد تأكيد المالك، ظهرت رسالة Vercel الرسمية `Updated Environment Variable successfully. A new deployment is needed for changes to take effect.` وتُظهر نافذة إعادة النشر أن البيئة المختارة هي **Production** وأن مصدر النشر هو نسخة `main` الحالية المعيّنة للنطاق `b2-rentmorocco.vercel.app`. لا يتضمن هذا السجل قيمة أي متغير أو كلمة مرور.

في 25 أغسطس 2026، أنشأت Vercel نشر Production جديداً بنجاح بعد الضغط على إعادة النشر المعتمدة. رابط متابعة النشر داخل لوحة Vercel هو `https://vercel.com/b2-rent/b2-rentmorocco/7t1HR6eQC3E8Q2yAJSuSbhysmRyx`. لم يُفحص الاتصال أو تُحوّل أي بيانات بعد؛ الخطوة التالية هي انتظار جاهزية النشر ثم إجراء طلبات صحة قراءة فقط.

أعاد أول فحص Production بعد النشر `GET /api/health/supabase` الحالة `503` مع `configured: false` و`status: not_configured`. هذه النتيجة لا تكشف أي سر، وتثبت أن `SUPABASE_DB_URL` لم يصل بعد إلى بيئة Production رغم حفظ إجراء واجهة سابق؛ لذلك لا توجد جاهزية اتصال ولا يجوز الانتقال إلى ترحيل بيانات أو اعتماد إنتاجي. يلزم إعادة إدخال رابط Transaction pooler في قيمة Production ثم حفظه والتحقق من ظهور نطاق Production قبل إعادة النشر.

تأكيد واجهة Vercel اللاحق أظهر أن `SUPABASE_DB_URL` ما زال معنونا بـ **Preview** فقط، في حين أن `SUPABASE_URL` معنونا بـ **Production**. لذلك لم يكن من الآمن افتراض أن رابط قاعدة البيانات قد حُفظ في Production، وتجب إضافة قيمة Production مستقلة له من خلال نموذج متغير جديد أو من خيار القيم السرية المنفصلة، ثم إعادة تحقق GET.

## طبقة Vercel Cron الآمنة — 25 أغسطس 2026

أضيف إعداد يومي للمسار `GET /api/cron/reconcile` في `vercel.json`، مع تحقق من ترويسة `Authorization` باستخدام `CRON_SECRET` ومقارنة ثابتة الوقت. في هذه المرحلة ينفذ المسار وضع `dry-run` فقط: لا يكتب في قاعدة البيانات، ولا يرسل بريداً أو واتساب، ولا يشغّل منطق التذكيرات أو مزامنة iCal المرتبطين حالياً بـMySQL/Manus.

الطلبات من دون السر، أو بسر غير مطابق، تتلقى `401` مقتصراً على `{ "ok": false, "error": "unauthorized" }` بلا أي تشخيص أو قيمة سرية. التغطية الاختبارية تتحقق من الغياب والخطأ والنجاح، ومن بقاء عدّادات التأثيرات الجانبية صفراً، ومن عدم ظهور السر في الاستجابة. لا يُضاف `CRON_SECRET` إلى Vercel من المصدر أو المستودع؛ يظل إدخاله الفعلي خطوة منفصلة وآمنة داخل إعدادات Production بعد مراجعة المستخدم.

في تحقق لوحة Vercel اللاحق، كان آخر نشر Production ظاهر مرتبطاً بالالتزام `e7319bb` فقط، بينما كان المستودع المرتبط يحتوي الالتزام الأحدث لطبقة Cron. لذلك أعاد المسار العام في Vercel مؤقتاً `404 Cannot GET /api/cron/reconcile`، وهي علامة على أن النشر الجديد لم يبدأ بعد وليست فشلاً في منطق التفويض. لا يُجرى قبول خارجي للمسار قبل ظهور نشر Production جديد مبني على الالتزام الأحدث ثم تحقق طلب GET غير موثق يعيد `401` الآمن.

### قبول النشر بعد مزامنة GitHub

بعد مزامنة الالتزام `207070b` إلى `main`، وصل النشر الجديد إلى نطاق Vercel. تحقق طلب `GET /api/cron/reconcile` غير الموثق من أن المسار موجود ويعيد `HTTP 401` كما يجب؛ وهو لا ينفذ أي عملية ولا يكشف سبب الرفض. كما أعاد `GET /api/health/supabase` حالة `HTTP 200` مع العقد الآمن `ok:true` و`configured:true` و`status:"ready"`، وباسم الخدمة `b2-rent-supabase-production` المتوافق مع هدف Vercel. لا تعني هذه النتائج أن طبقات Auth أو Storage أو Email أو Vision أو تشغيل حجوزات PostgreSQL أصبحت جاهزة؛ يبقى مسار Cron في وضع `dry-run` إلى حين اكتمال تلك البدائل واختبارات القبول المناسبة.

## طبقة Supabase Storage الخاصة — تحضير فقط

أضيف adapter خادمي مستقل في `server/supabasePrivateStorage.ts` إلى جانب التخزين الحالي، ولم يُربط بمسارات رفع أو تنزيل أو بـ`tRPC`. يقتصر دوره الآن على إنشاء مفاتيح ملفات غير كاشفة للاسم الأصلي ضمن مساحة `b2rent/private/<kind>/<subject>/<uuid>.<ext>`، وقبول ثلاثة أصناف فقط: `kyc` و`contract` و`dispute`. لا تُنقل الملفات ولا تُنشأ buckets ولا تُكتب سياسات ولا يُستدعى Supabase عبر الشبكة ضمن هذه المرحلة.

يفرض adapter أن تكون روابط التنزيل الموقعة مؤقتة بين 60 و900 ثانية، وألا تصدر إلا لمفتاح يطابق المساحة الخاصة التي يولدها التطبيق. كما يرفض معرفات المسار غير الآمنة ومحاولات traversal قبل أي اتصال شبكي، ولا يضع مفتاح الخدمة في الاستجابة أو السجل. هذا ينسجم مع نموذج buckets الخاصة في Supabase، الذي يحمي الملف عبر سياسات الوصول ويوفر الروابط الموقعة المؤقتة للوصول المنضبط.[4] [5]

لا يعني وجود هذا adapter أن التخزين الإنتاجي مفعل. قبل أول اختبار كتابة معزول، يلزم أن ينشئ المالك bucket خاصاً باسم `b2rent-private-documents` في Supabase، ويطبق سياسة RLS تقيّد الملفات، ثم يضيف `SUPABASE_SERVICE_ROLE_KEY` إلى Vercel Production من واجهة الأسرار فقط. بعد ذلك يجرى اختبار لقبول ملف وهمي غير شخصي في Preview، وتثبت عملية التحميل والرابط الموقّع ومنع الوصول العام، قبل التفكير في نقل أي وثيقة حقيقية.

### فحص صحة Storage للقراءة فقط — مقبول في Production

أضيف المسار العام `GET /api/health/storage` كفحص metadata-only. يستدعي هذا المسار endpoint معلومات الـbucket في Supabase بطريقة `GET` خادمية فقط؛ لا يرفع كائناً، ولا يسرد الملفات، ولا ينشئ رابطاً موقّعاً، ولا يحذف أي شيء. تعيد الاستجابة الحقول العامة `ok` و`configured` و`status` فقط، وتختزل الأخطاء إلى `not_configured` أو `bucket_not_found` أو `unavailable` دون إعادة رسائل Supabase أو مفتاح الخدمة.

تثبت اختبارات Vitest أن الاتصال عند وجود الإعداد يستخدم `GET` لمسار metadata الخاص بالـbucket فقط، وأن غياب الإعداد يمنع أي طلب شبكي، وأن المفتاح لا يظهر في الصحة العامة. بعد نشر الالتزام `76acc16` إلى Vercel ونجاح GitHub Actions، أعاد `GET /api/health/storage` في Production `HTTP 200` مع `ok:true` و`configured:true` و`status:"ready"`. يؤكد ذلك أن الـbucket الخاص والمفتاح الخادمي متاحان للخادم فقط؛ ولا يثبت رفع ملفات أو الوصول إلى محتوى. يبقى اختبار الرفع ثم الحذف لملف وهمي مؤجلاً لأن العملية تحتاج استدعاء خادمياً داخلياً مصادقاً، لا endpoint عاماً يمكن أن يطلقه أي زائر.

## مراحل الترحيل المقترحة

### 1. فصل البنية والبيانات

يُنشأ مثيل MySQL إنتاجي مستقل، ثم تُراجع migrations من Drizzle وتُنفذ في بيئة اختبار أولاً. بعد استيراد بيانات صالحة ومصرح بها، يضبط `DATABASE_URL` في بيئة Preview، ثم يختبر مسار قراءة عام ومصادقة غير متلفة. لا تُنقل وثائق الهوية أو أدلة النزاعات إلا بعد مراجعة سياسة الاحتفاظ والملكية.

### 2. نقل الهوية والجلسات

يفصل OAuth الخاص بـManus عن كود الإنتاج الخارجي. يتطلب ذلك مزود هوية مستقل، التسجيل الصحيح لعنوان callback في نطاق Vercel، سر جلسات جديد، وتدقيق خصائص cookie (`Secure` و`HttpOnly` و`SameSite`). لا يُقبل تسجيل دخول حقيقي قبل إثبات: تسجيل الدخول، تسجيل الخروج، تجديد الجلسة، ومنع callback غير الصحيح.

### 3. تخزين الملفات وفحص الصور

تستبدل طبقة Forge بمخزن ملفات متوافق مع S3 وطبقة رفع ذات عناوين موقعة. تبقى قيود إثبات فحص الصور الخادمي قائمة، لكن يستبدل مزود Vision AI مع سياسة واضحة لرفض الصور التي لا تحقق المصداقية. تُفحص صلاحيات الملفات الحساسة يدوياً بعد كل انتقال.

### 4. الإشعارات والعمليات الدورية

بعد وجود قاعدة البيانات والهوية، تُنقل تذكيرات العقود ومزامنة iCal إلى endpoint مجدول محمي بـ`CRON_SECRET` مع قفل موزع ومعالجة idempotent. تقوم Vercel باستدعاء المهام المجدولة عبر `GET` على endpoint إنتاجي، ولا تعيد المحاولة تلقائياً عند الفشل؛ كما يمكن أن تتكرر الدعوات أو تفوت، ولذلك يلزم تصميم مبني على التسوية وإعادة المعالجة لا على عدّادات تراكمية.[2] [3]

إذا كان المشروع على خطة مجانية في Vercel، فالجدولة تقتصر على تشغيل يومي وقد تُنفّذ في أي وقت ضمن الساعة المحددة؛ هذا لا يفي بتنبيه قبل 48 ساعة بدقة أو تزامن iCal دوري قصير. حينها يبقى مسار الاستضافة المتكاملة الحالي أو خدمة خلفية دائمة خياراً أكثر ملاءمة للمهام المتكررة.

### 5. اختبار القبول قبل تغيير الرابط الرسمي

يجب تنفيذ الاختبارات التالية على نطاق Preview مستقل أولاً، وبحسابات اختبار معزولة فقط:

1. `GET /api/health` يعيد `200` ولا يعرض أسراراً أو قيم متغيرات.
2. عرض قائمة فعلية من قاعدة بيانات الاختبار، ثم عرض تفاصيل عرض دون كشف رقم WhatsApp قبل تأكيد الحجز.
3. OAuth: تسجيل دخول وخروج، callback غير صحيح، ورفض CSRF.
4. رفع صورة اختبار، فحصها، ثم رفض ملف غير صالح؛ مع التحقق من عدم عمومية الملفات الخاصة.
5. حجز اختباري مع تفادي الدفع الحقيقي، توليد فاتورة/عقد، وحماية الوصول وفق الدور.
6. تشغيل iCal والتنبيه المجدول مرتين للتحقق من القفل وعدم التكرار، ثم اختبار الاستعادة بعد فشل متعمد غير مدمر.
7. قياس مسار الهاتف والشاشة الكبيرة، ومراجعة سجلات الأخطاء بعد 24 ساعة من الاختبارات.

بعد اجتياز هذه البنود فقط، تُنقل المتغيرات المراجعة إلى بيئة Production، ويعاد النشر، ثم يحدَّث النطاق الرسمي مع وجود خطة رجوع إلى النسخة المتكاملة الحالية.

## مراجع

[1]: https://vercel.com/docs/environment-variables "Vercel — Environment Variables"

[2]: https://vercel.com/docs/cron-jobs "Vercel — Cron Jobs"

[3]: https://vercel.com/docs/cron-jobs/manage-cron-jobs "Vercel — Managing Cron Jobs"

[4]: https://supabase.com/docs/guides/storage/buckets/fundamentals "Supabase Storage — Buckets Fundamentals"

[5]: https://supabase.com/docs/guides/storage/serving/downloads "Supabase Storage — Serving Downloads"
